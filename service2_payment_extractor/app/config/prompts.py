# =============================================================================
# 业务词典单源化（P0-3 / P2/P3）声明
# =============================================================================
# 本文件中所有"安装节点白名单 / 跨类映射 / 关键词列表"的字面文字都已在
# `app/resources/business_dict/v1.yaml` 中维护一份只读副本。
#
# P2/P3 修复：设备/安装标准节点列表不再在此硬编码，改由
# `app.config.prompts_loader.load_prompts()` 从 `get_business_dict()` 动态注入。
# 启动期 `app.config.business_dict.assert_consistency_with_prompts()`
# 会校对覆盖率，发现漂移立即告警 / 拒启（prod）。
#
# 修改任一节点文字时只需更新 v1.yaml；prompts_loader 会自动读取。
# =============================================================================
#
# =============================================================================
# 占位符语法约定（M4）
# =============================================================================
# 本文件中两套占位符语法并存，请勿混用：
#
# 1) `{{name}}` 双大括号：由 `app.config.prompts_loader.render()` 在加载期替换。
#    用途：模板组合 / 业务词典占位符注入。
#    示例：`_PAYMENT_RATIO_PROMPT_COMMON` 中的 `{{payment_clause}}`。
#
# 2) `{name}` 单大括号：由 LangChain `PromptTemplate` 或 `str.format()` 在
#    调用期替换。
#    用途：业务调用现场的最终变量注入（条款列表、ID、摘要等）。
#    示例：`PAYMENT_CLAUSE_VALIDATION_PROMPT` 中的 `{validation_clauses}`。
#
# 当 `{{...}}` 与 `{...}` 同时出现时，loader 必须先于 format 执行；
# 单大括号变量名禁止与双大括号变量名相同，避免一次替换后被二次误替换。
# 模板中如需输出字面 `{` / `}`，必须写成 `{{` / `}}` 转义；
# 此时 loader 会忽略（因为没有匹配的占位符名），LangChain f-string 引擎会按
# f-string 规则解析为字面括号。
# =============================================================================

_PAYMENT_RATIO_PROMPT_COMMON = """
你是合同支付条款分析专家，精准提取**当前支付条款**中的**所有付款节点**。
以下规则中的「原文」均指本次任务输入末尾的 `payment_clause`；「上下文」均指 `current_clauses_chunk`。

---

## 核心规则
### ⚠️ 节点来源硬约束（最高优先级，违反即任务失败）

1. **句子级溯源**：把原文切分为独立支付动作单元（每个"触发条件 + 比例/金额"组合 = 1 个单元），节点数 N = 单元数；JSON 数组长度**必须 = N**。
2. **触发文字定位**：每个节点的触发条件文字与比例/金额文字必须能在原文中指出；指不出 → 立即丢弃该节点。
3. **上下文用途边界**：
   - ✅ 允许：用上下文推断"前序累计基线"、识别互斥分支、判定"唯一总价"。
   - ❌ 严禁：把上下文中其他条款的触发文字、payment_type、比例数字搬到本次输出，无论它们多像一个"合理节点"。

**反例（必须背下）**：
- 原文 = "甲方于乙方进场前 30 日将…30% 支付给乙方作为进度款…乙方收到进度款后按商定的开工日期进场安装支付至 50%。"
  - 上下文中即使出现"剩余结算价款"、"工程整体竣工"、"50% 余款"等文字 → **严禁**输出 `工程整体竣工 50%`；该节点的触发文字不在原文里。
  - 正确输出 2 个节点（顺序按原文出现先后）：进场前（首付）30%，进场后 20%（**50%−30%=20%**，禁止直接采用 50%）。
- 原文仅含 1 个支付动作 → 即使上下文有 3 个节点也只输出 1 个。

### 0. 多触发条件识别

原文形如以下形态时，**必须输出 ≥ 2 个节点**：
- "累计支付至 X%。结算/审定后，付至 Y%" → 节点1: ratio=X%−前序累计；节点2: ratio=Y%−X%
- "支付至 X%。X年后/质保期满后付清尾款" → 节点1: ratio=X%−前序累计；节点2: 质保金, ratio=100%−X%
  ⚠️ **"余款N%在质保期满/保修期满后支付"是质保金节点**，不是工程整体竣工——触发条件"质保期满"指向质保金返还，不指向竣工。
  ⚠️ 示例：无前序条款，原文"…验收合格…付至安装结算价的95%，余款5%在质量保修期满后支付" →
    节点1: 当地政府部门验收后 **95%**（前序=0%，95%−0%=95%）；节点2: 质保金 **5%**（100%−95%=5%）
- **"支付至 X%，N% 余款/尾款作为质保金/质量保修金"，或"支付至X%，质保金的N%...返还/无息返还"（同句含质保金扣留）** →
  节点1: payment_type=（按触发条件判断），ratio=X%−前序累计；
  节点2: payment_type=质保金，ratio=N%（直接采用原文 N%，**不**再做差额）。
  ⚠️ 匹配关键词：`"N%余款作为质保金"` / `"N%作为质量保修金"` / `"质保金的N%...返还"` / `"质保金N%...无息返还"` / `"N%质保金...保修期后支付"`
  ⚠️ 示例1：前序80%，原文"支付至结算价的97%，3%余款作为质量保修金" →
    节点1: 工程整体竣工 **17%**；节点2: 质保金 **3%**
  ⚠️ 示例2：前序80%，原文"支付至审计金额的97%，质保金的3%在保修期结束且建设单位资金到位后无息返还" →
    节点1: 结算完成 **17%**（97%−80%）；节点2: 质保金 **3%**（"且...后"等附加条件不影响质保金节点提取）
- **"进场前 N% 进度款 / 预付款 + 进场/开工后支付至 M%" 复合首付模式** →
  节点1: payment_type=进场前（首付）, ratio=N%（直接采用原文 N%）；
  节点2: payment_type=进场后, ratio=**M%−N%**（"支付至 M%" 是累计目标，必须做差额）。
  ⚠️ 工作示例：原文"进场前 30% 进度款…进场后支付至 50%" → 节点1: 进场前（首付）30%；节点2: 进场后 **20%**（50−30=20）。
  ⚠️ 即使两个动作在同一句子或同一编号下，也必须输出 2 个独立节点；
  ⚠️ N% 是当前条款自身的支付动作，**禁止**当作"前序累计基线"丢弃节点1；M% 是累计目标，**禁止**直接当节点2 的 ratio。
  ⚠️ 节点输出顺序按原文出现先后：进场前 节点 在前，进场后 节点 在后。
- **"预付款/定金为总金额的 N% 即X元 … 验收合格后/满足后续条件后支付总价的 M% 即Y元" 直接比例双节点模式** →
  节点1: payment_type=预付款/定金, ratio=N%, amount=X元（直接采用原文 N% / X元）；
  节点2: payment_type=（按触发条件判断，如"当地政府部门验收后"）, ratio=M%, amount=Y元（直接采用原文 M% / Y元）。
  ⚠️ N% 和 M% 都是**直接比例**，不是"支付至"累计比例，**禁止**做差额计算；只有出现"支付至/付至/累计至"字样时才允许按累计规则换算。
  ⚠️ 即使两个动作位于同一句子、同一编号、同一个逗号分句中，也必须拆成 **2 个独立节点**。
  ⚠️ **禁止**因为第二个节点带有更明确的触发条件（如"当地政府部门验收后"），就忽略前面的"预付款/定金"节点；前面的 N% 本身就是独立支付动作，必须保留。
  ⚠️ 工作示例：原文"预付款为总金额的40%即53360元，验收合格后支付总价的60%即80040元" → 节点1: 预付款 **40%**、53360元；节点2: 当地政府部门验收后 **60%**、80040元。
  ⚠️ 节点输出顺序按原文出现先后：预付款/定金 节点在前，后续验收/交付/结算节点在后。

### 1. 节点识别原则
- 每个独立"触发条件 + 比例/金额"= 1 个节点
- 同动作补充说明不拆分（"30%，即60万元" → 1个节点）
- 隐含质保金（"剩余X%作为质保金"无独立"支付"动词）也单独提取；**"质保金的N%...返还/无息返还"也视为质保金节点**（"返还"即支付动作）
  ⚠️ 示例："质保金的3%在保修期结束后无息返还" → 质保金节点，ratio=**3%**

### 2. 提取目标
- **payment_type**: 从标准节点列表选择：{standard_node_list}
- **amount**: 明确货币金额（如 "50万元"），无则 `null`
- **ratio**: 单次应付百分比

### 3. 比例计算
- 直接比例："支付X%" → 直接采用
- 累计转换："支付至X%" → **本次 ratio = X% − 前序累计**（前序累计 = 本条款**之前**各条款的 ratio 之和）
  ✅ 正例：上文(1)预付款ratio=10%，本条款"支付至60%" → 前序=**10%**，ratio=60%−10%=**50%**
  ❌ 错误：把ratio结果50%当前序 → "前序50%，60%−50%=10%" —— 严禁此类颠倒！
- 隐含剩余："付清余款" → 100% − 前序累计
- **前序累计基线**：只统计本条款**之前**出现的条款 ratio（带 `%` 的支付比例）；"X个工作日"等期限数字**不是**支付比例。
  ⚠️ **无前序条款时前序累计 = 0%**：若本条款之前不存在已确认的付款条款（上下文中无其他带 `%` 的支付动作），则前序累计 = 0%，"支付至 X%" → ratio = **X%**（禁止凭空假设前序比例）。
  ❌ **严禁从 RAG 示例或条款中的非支付描述推断前序累计**：如原文提到"进场安装"但无"进场前支付X%"的付款动作，**禁止**假设存在 30% 进场前付款作为前序累计。

### 3.1 互斥分支识别（最高优先级，避免分支间累加）
当上下文出现以下结构时，并列编号 `(1)(2)(3)` / `1)2)3)` / `①②③` / `方式一/方式二` 的支付方案视为**互斥分支**：
- 触发措辞："采用以下第 N) 种方式" / "任选其一" / "二选一" / "以下方式中选一" / "或" 连接的并列付款表达
- **前序累计只统计互斥分支之外的已确认节点**（如"进场后 50%"、"验收后 30%"），**严禁将其他分支的比例计入前序累计**
- 例：上下文有"进场后 50%"、"验收后 30%"，分支(1)"支付至 97%"、分支(2)"支付至 100%"
  - 处理(1)"支付至 97%"：前序 = 50%+30% = 80% → ratio = 17%（**不能**因(2)出现 100% 就错算）
  - 处理(2)"支付至 100%"：前序 = 50%+30% = 80% → ratio = 20%（**严禁**把(1)的 97% 当前序，得出 3%）

### 4. ratio 决策树（按序首个命中即落定）
1. 原文含"支付至 X% / 累计至 X%" → ratio = X% − 前序累计（禁止直接用 X%）
2. 原文含"支付给…合同价款/结算价的 X%"或"支付…的 X%"（**无**"支付至"字眼），且 X% > 前序累计，且上下文前序已出现"支付至Y%"递增模式 → 推断为隐式累计，ratio = X% − 前序累计
   ⚠️ 示例：前序已"支付至60%"，本条款"支付给乙方安装完成电梯数量对应合同价款的80%"，80%>60% → ratio = **20%**；禁止输出80%
3. 原文含"余款/尾款/付清/结清"且同时含以下任一质保金扣留描述 → ratio = 100% − 前序累计 − N%（**不**另产生质保金节点）：
   - "扣除 N% 质保金" / "扣除合同总价 N% 的质保金" / "扣除审计后合同总价 N% 的质保金" / "扣留 N% 作为质保金" / "质保金 N%…余款"
   - 示例：前序30%，原文"扣除审计后合同总价5%的质保金及已支付款项后的余款" → ratio = **65%**（100%−30%−5%）
4. 原文含"付清/结清/余款/尾款"（无质保金扣除说明）→ ratio = 100% − 前序累计
5. 隐含全额支付：原文明确"无预付款"或"无进度款"或"一次性付款"，且描述验收/审核后完成全部款项支付 → ratio = "100%"
6. 仅有金额无比例：上下文正文中存在**明确标注的**唯一总价（如"安装费合计：X万元"/"合同金额：X元"/"安装总价 X 万元"等带标签的表述）且口径一致 → 反算比例；否则 `ratio=null`
   ❌ **严禁从表格数值、设备规格参数、零散数字中推断总价反算比例**。
   ❌ **严禁从 RAG 示例借用比例**：RAG 示例中同类型节点带的比例（如"进场前（首付）: 30%"）**绝对不能**套用到当前条款。
   ❌ 反例：原文"支付安装费 26000 元"（无任何 % 字样，上下文仅有表格数值无明确标注总价）→ `ratio=null, amount="26000"`，**禁止**输出 `ratio="30%"` 或 `ratio="50%"`
7. 金额与比例并存 → 同时输出
8. 原文含显式 X%（以上均不命中）→ 直接采用

⚠️ 输出非 null ratio 前，必须在原文指出 `%`/`百分之`/"付清/余款"字样或上下文唯一总价；三者皆无 → `ratio=null`。**严禁**经验直觉填充，**严禁从 RAG 示例或经验借用比例数字**。

### 5. 无效条款（输出 `null`）
- 纯标题/章节标记
- 保函/担保（现金质保金返还除外）
  - 反例（无效）：「结算额 5% 的质保金保函」/ 「提供银行保函替代质保金」/ 「开具等额质保保函」—— 描述的是凭证，不是现金支付动作 → `null`
  - 例外（有效）：「5% 质保金…满 2 年后支付；**若提供质保金保函则付至 100%**」—— 原文含"付至 100%"现金支付动作 → 有效（见 §节点判断注意事项 保函替代规则）
- 程序/凭证/工具/概述（无金额比例）
- 否定/拒付/残缺条款
- **表格汇总行重复付款链**：若原文是 Markdown 表格行或类似表格汇总行（如 `| 商务要求 | 付款要求 | ...`）
  - 识别特征：`|` 表格分隔符 + 表头词（"商务要求"/"付款要求"等）

## 节点判断注意事项
{judgement}

## 关键约束
- ratio / amount **严格来自原文**；禁止借 RAG 数值或经验直觉填充。
- 支付："支付30%"/"支付余款"；非支付："提供5%保函"/"开具发票"

## 输出格式
**只输出** JSON 数组或 `null`，无任何前导/解释文字。长度 = N。
若展示比例推理，**必须**使用以下格式（先列前序，再做差）：
`比例提取：前序条款=(序号)ratio=X%+(序号)ratio=Y%=…，前序累计=Z%，本次=目标%−Z%=W%`

{output_examples}

字段：
- **payment_type**: 标准节点列表中选择
- **ratio**: `"30%"` 格式或 `null`
- **amount**: 数字/字符串或 `null`
- 命中§5 → 输出 `null`

---

## 待分析信息（请基于以下输入应用上述规则）

- **当前支付条款 (payment_clause)**：
{{payment_clause}}

- **条款类型参考 (payment_type, RAG 多数票)**：{{payment_type}}

- **前序条款上下文 (current_clauses_chunk, 仅供累计基线/互斥分支/唯一总价判定使用)**：
{{current_clauses_chunk}}

- **RAG 参考示例 (rag_examples, 仅供 payment_type 归类参考，禁止借数值)**：
{{rag_examples}}

请开始分析：```
"""

# ===== 设备付款条款标准节点列表 =====
# P2/P3: 节点列表已迁移至 v1.yaml equipment.payment_type_whitelist，
# 由 prompts_loader 在组装时动态注入。此处保留 _EQUIPMENT_STANDARD_NODES
# 仅作为 prompts_loader 不可用时的兜底（如单元测试直接 import）。
_EQUIPMENT_STANDARD_NODES = "1.销售定金 | 2.预付款 | 3.提货款 | 4.货到工地 | 5.安装后 | 6.公司验收后 | 7.当地政府部门验收后 | 8.结算完成 | 9.特殊约定付款-移交前 | 10.电梯移交用户后 | 11.特殊约定付款-移交后 | 12.工程整体竣工 | 13.质保金"

_EQUIPMENT_OUTPUT_EXAMPLES = """```json
[{{"payment_type": "预付款", "ratio": "10%", "amount": null}}]
```
多节点：
```json
[{{"payment_type": "销售定金", "ratio": "10%", "amount": null}}, {{"payment_type": "当地政府部门验收后", "ratio": "15%", "amount": null}}, {{"payment_type": "结算完成", "ratio": "5%", "amount": null}}]
```"""
_EQUIPMENT_JUDGEMENT = """
1. 含"专项维修基金/政府补贴/国债补贴/审计审价完成/**超长期特别国债**/国债资金到位" → **工程整体竣工**（除非最终限制条件是"货到工地" → **货到工地**）
2. "设备到货前X天支付" → **提货款**（非预付款）
3. "移交款...支付审定总价" → **结算完成**
4. "移交手续/甲方接收/移交用户/移交物业/移交钥匙" 优先级 > 验收措辞 → **电梯移交用户后**
5. "预付款保函+定金"组合 → **销售定金**（非预付款）
6. "交货验收/开箱验收" → **货到工地**
7. "办理结算手续后" → **结算完成**
8. 连续"支付至 X%"递增 → 每段独立成节点（首段 X%，第二段 Y%-X%）
9. 前序累计 <100% 时，"X年后付清尾款" → 新增 **质保金** 节点，ratio=100%-前序累计
10. **保函替代质保金规则（优先级最高）**：
  - 模式："N% 作为质量保证金…X 年/月后支付；**若乙方提供质保金保函/银行保函，付至 M%**" →
    节点判 **工程整体竣工**，ratio = M% − 前序累计（保函替代现金扣留，实际竣工时付完）。
    ❌ **不**判 质保金——质保金节点仅用于"无保函选项、纯现金扣留 N 年后支付"的情形。
  - 模式："提供/开具质保金保函 N%"，原文**无**独立"支付/付款"现金动作 → **无效条款**（§5 保函类），输出 `null`。
    反例（无效）：「结算额 5% 的质保金保函」—— 只描述开具凭证，没有现金支付 → `null`。
"""


# ===== 安装付款条款标准节点列表 =====
# P2/P3: 节点列表已迁移至 v1.yaml install.payment_type_whitelist，
# 由 prompts_loader 在组装时动态注入。此处保留 _INSTALL_STANDARD_NODES
# 仅作为 prompts_loader 不可用时的兜底（如单元测试直接 import）。
_INSTALL_STANDARD_NODES = "1.定金 | 2.进场前（首付）（关键词：\"开工前\",\"设备进场前\",\"书面进场通知\"） | 3.进场后（关键词：\"合同设备进场后\",\"设备进场\"） | 4.移交前 | 5.报验前 | 6.公司验收后（关键词：\"厂家验收\"，\"乙方验收\"，\"甲方验收\"） | 7.当地政府部门验收后（关键词：\"政府验收\", \"特种设备验收\", \"技监部门验收\", \"监督检验合格\"） | 8.电梯移交用户后（关键词：\"移交用户\", \"交付使用\"） | 9.工程整体竣工（关键词：\"剩余结算价款\", \"付清所有款项\", \"工程结算\", \"支付剩余款项\", \"结算完成\", \"审定结算款\"） | 10.特殊付款-移交前 | 11.特殊付款—移交后 | 12.质保金（关键词：\"质量保证金\", \"尾款\"）"

_INSTALL_OUTPUT_EXAMPLES = """```json
[{{"payment_type": "进场前（首付）", "ratio": "30%", "amount": null}}]
```
多节点：
```json
[{{"payment_type": "当地政府部门验收后", "ratio": "30%", "amount": null}}, {{"payment_type": "工程整体竣工", "ratio": "15%", "amount": null}}]
```"""
_INSTALL_JUDGEMENT = """
1. "书面进场通知" → **进场前（首付）**
2. "厂家/甲方/乙方验收（合格/后）" → **公司验收后**（必须是已完成验收语义）
3. "X验收前/报验前/报检前" → **报验前**（验收动作未发生）
4. "结算完成/结算完成后/审定结算款/办理结算手续后/审计审价完成/结算金额/结算价款" → **工程整体竣工**
   ⚠️ **此规则最高优先**：一旦 payment_clause 中出现上述任一词，无论条款中是否同时出现"特检/验收/取证"等其他条件，**必须判为工程整体竣工**，不得改判为其他节点。
   ✅ 典型例：「取得特检合格证，结算完成后 20 天内，支付结算金额的 15%」→ 含"结算完成后"+"结算金额" → **工程整体竣工**
4.5 **白名单冲突优先级**：当 `{{payment_clause}}` 同时命中节点7「当地政府部门验收后」关键词（"特种设备验收/政府验收/技监部门验收/监督检验合格/取得特检合格证/特检合格"）与节点9「工程整体竣工」语义（"剩余结算价款/付清所有款项/工程结算/支付剩余款项/结算完成/结算完成后/结算金额/审定结算款"）时：
  - 若含「结算完成后 / 结算金额 / 剩余 / 余款 / 结算款 / 付清所有款项」 → 判 **工程整体竣工**；
  - 若仅为验收后立即支付的中间款（无结算完成/余款语义） → 仍判 **当地政府部门验收后**。
  ✅ 正例：「取得特检合格证，结算完成后 20 天内，支付结算金额的 15%」→ 含「结算完成后」+「结算金额」→ **工程整体竣工**
  ✅ 正例：「特种设备验收合格后…甲方支付剩余的结算价款」→ 含「剩余结算价款」→ **工程整体竣工**
  ❌ 反例：「取得特检合格证后 10 天内支付 40%（无结算语义）」→ **当地政府部门验收后**
5. 占位金额（如"￥___元"）不进入分母、不构成前序累计
6. 仅一条条款含金额且 == 上下文唯一安装总价 → ratio=100%（勿因"余额"措辞或习惯输出 50%）
7. 连续"付至 X%"递增 → 每段独立成节点
8. 前序累计 <100% 时，末段"取证满X年后/验收合格X年后/免保期满" → 新增 **质保金**, ratio=100%-前序累计
9. **质保金与保函判定**：
    正例：「剩余安装款的 5% 作为质量保证金在工程竣工满 2 年后 60 日内支付。若乙方提供质保金保函付至 100%」→ **质保金 5%**。
    反例（无效）：
    「结算额 5% 的质保金保函」—— 仅开具凭证，无现金支付动作 → `null`。
    「质保金：结算额 5% 的质保金保函」—— 即使有"质保金"标题，正文只描述保函，无现金支付 → `null`。
"""

# ===== 最终拼装：设备 / 安装 各自独立的提示词 =====
# P2/P3: 此处拼装使用硬编码节点列表作为兜底；正式运行时 prompts_loader
# 会用 BusinessDict 动态注入的节点列表重新拼装，覆盖此处的值。
EQUIPMENT_PAYMENT_RATIO_PROMPT = _PAYMENT_RATIO_PROMPT_COMMON.format(
    standard_node_list=_EQUIPMENT_STANDARD_NODES,
    output_examples=_EQUIPMENT_OUTPUT_EXAMPLES,
    judgement=_EQUIPMENT_JUDGEMENT,
)

INSTALL_PAYMENT_RATIO_PROMPT = _PAYMENT_RATIO_PROMPT_COMMON.format(
    standard_node_list=_INSTALL_STANDARD_NODES,
    output_examples=_INSTALL_OUTPUT_EXAMPLES,
    judgement=_INSTALL_JUDGEMENT,
)

# 向后兼容：保留 PAYMENT_RATIO_PROMPT 别名，默认指向设备版本
PAYMENT_RATIO_PROMPT = EQUIPMENT_PAYMENT_RATIO_PROMPT



PAYMENT_SUMMARY_RATIO_PROMPT = """# 设备付款条款批量复核与标准化

## 1. 角色与目标
你是精通合同、法律与财务的分析专家。对输入的批量**设备侧**付款条款进行：过滤 → 计算 → 金额 → 合并去重 → 类型标准化 → 校验，全链路处理后输出结构化 JSON。

## 2. 核心铁律（违反即任务失败）
1. **原文不可改**：`payment_clause` 逐字来自输入，禁止改写/截断/编造。
2. **ID 对应**：输出 `id` 必须来自输入，不得新造或遗漏。
3. **数量控制**：输出节点数 ≤ 输入节点数，只能"过滤 / 合并"，不得拆分。
4. **禁止外部引用**：`chunk_text_map` 与 `rag_examples` 仅供理解，禁止出现在输出字段。
5. **节点字段必须来自 payment_clause 原文（最高优先级）**：每个输出节点的 `payment_type` / `final_ratio` / `final_amount` 三个字段，**必须能在该节点自身的 `payment_clause` 原文中找到直接文字依据**。`chunk_text_map`（其它条款）和 `rag_examples` 仅用于理解任务/格式与"支付至 X%"的累计比例计算基线，**严禁**从中"借"节点的分类标签、比例数值或金额数值。
   - ❌ 反例：当前 `payment_clause` 是"4.4 甲方应直接向本合同约定乙方账户支付所有款项..."（程序性泛化条款），即使 `chunk_text_map` 中有 4.3 的"质保金 5%"或 4.1 的"RMB:62550 元"，**不**能输出 `payment_type=质保金, final_ratio=5%, final_amount=62550`。这种条款若 `payment_clause` 自身不含具体节点信息，应在 Step 1 过滤掉。
   - ❌ 反例：当前 `payment_clause` 是"4.2...支付至审定价格"（被截断、不含具体比例和金额），不得从 `chunk_text_map` 借"95%"或"62550 元"补全到 final_ratio / final_amount；该节点应输出可证实的最简结果（仅触发条件可识别时，比例由"支付至 X%"差额计算给出，金额若原文不含则为 `null`）。
6. **过程可追溯**：数组**最后**一个对象必须为 `{{"thinking_output": "..."}}`。
7. **`结算完成` **：`
   - ❌ **典型反例（必须背下）**：`payment_clause="验收通过后支付剩余款项"` → **不是结算完成**。原文只有"验收"二字，没有"审"字，应判 `当地政府部门验收后`。
   - ❌ **典型反例**：`payment_clause="验收合格后支付余款"` / `"项目验收后支付剩余尾款"` / `"验收后付清"` → **均不是结算完成**，应判 `当地政府部门验收后` 或 `工程整体竣工`。
   - ✅ 正例：`payment_clause="通过审价后支付审定总价扣除已支付款项后的余款"` → `结算完成`（含"审价""审定总价"）。

## 3. 处理流水线

### Step 1 | 无效节点过滤（命中任一即丢弃）
1. **0 元一票否决**：文本含 `0元 / 零元 / RMB0元 / 零元整` 等明示金额为 0。
   - 例："5%的合同定金,即人民币(大写)：零元整元整(RMB0元" → 丢弃。
2. **保函类**：各类保函（预付款/履约/质保/保修）的提交、替换、返还。
   - 例："甲方有权要求乙方重新开具合同价3%的保修保函" → 丢弃。
3. **仅总价**：只描述总价，无具体支付节点。
   - 例："产品含税总价：¥443,240元。" → 丢弃。
4. **空泛付清**：仅含"付清合同约定的应付款项 / 付清余款"，无条件或比例。
   - 例："委托方付清合同约定的应付款项" → 丢弃。
5. **概述引导**：仅描述付款安排方式，无具体金额/比例/单次动作。
   - 例："分期分批供货、付款、结算" → 丢弃。
   - 例："产品如分批次验收，委托方按每批次比例分批支付施工费余额" → 丢弃（仅描述分摊方式，无具体比例/金额）。
6. **付款凭证/通知类程序条款**：主要内容为付款后的凭证传真/寄送、注明合同编号、以银行凭证为准等手续性描述，即使含"支付"字样也丢弃。
   - 例："委托方应在付款后，将付款凭证传真或寄送至受托方的合同经办人，并注明付款单位名称、合同编号" → 丢弃。
7. **残缺片段**：语义不完整，无法独立成节点。
   - 例："求的,甲方有权不支付款项。" → 丢弃。
8. **拒付类**：含"不支付 / 有权拒付 / 暂停支付"等否定表述 → 丢弃。

### Step 2 | 比例计算（累计还原）
- "支付**至** X%" 或 "结清**至** X%"：`本次 final_ratio = X% − 前序已保留节点累计比例`（结果 ≥ 0）。
- 其他情况：直接取原文比例。

### Step 3 | 金额核算
- 有 `reported_amount` → 直接使用其值。
- 无 `reported_amount` → `final_amount = null`。
- 金额为 0 的节点一律过滤（与 Step 1.1 一致）。

### Step 4 | 合并与去重
1. **质保金强制合并**：所有 `payment_type = 质保金` 的节点合并为一个——`final_ratio` 累加、`final_amount` 累加、`payment_clause` 用 `\\n` 拼接、保留首个 `id`。
2. **多合同号去重**：若多节点 `payment_type + final_ratio` 相同（仅合同号不同），仅保留一个代表性节点。
   - 例：合同号 `25N3S08-PTP` 和 `25N3S08-PTL` 都是"提货款 95%" → 合并为单个"提货款 95.0%"。
3. **上游切分碎片合并（重要）**：若两节点 `final_ratio` 相同，且其中一条 `payment_clause` 是另一条的**文本子串或高度重合**（即同一意思的完整版与碎片版同时出现），视为上游条款切分重复，**合并为一个**——保留 `payment_clause` 更长且更完整的那一条，丢弃碎片那条。
   - 触发判定优先于 Step 5 的类型重分类：合并必须先于类型变更，避免碎片版被改判成另一类型导致漏去重。
   - 例：eq_A `payment_clause="当地政府验收通过后支付剩余款项"`（30%）与 eq_B `payment_clause="验收通过后支付剩余款项"`（30%）→ eq_B 是 eq_A 的子串 → 仅保留 eq_A，丢弃 eq_B。
   - 例：eq_A `payment_clause="货到工地支付合同总价的50%作为提货款"`（50%）与 eq_B `payment_clause="支付合同总价的50%"`（50%）→ eq_B 是碎片 → 仅保留 eq_A。

### Step 5 | 支付类型标准化
**标准类型（共 13 类，按优先级从左到右）**：
1.销售定金 | 2.预付款 | 3.提货款 | 4.货到工地 | 5.安装后 | 6.公司验收后 | 7.当地政府部门验收后 | 8.结算完成 | 9.特殊（移交前） | 10.电梯移交用户后 | 11.特殊（移交后） | 12.工程整体竣工 | 13.质保金

**特殊判定（优先级高于字面关键词匹配）**：
- **销售定金**：含"预付款保函" + "定金/订金"的组合（例："甲方收到乙方预付款保函后30天内支付合同总价10%的订金"）→ 销售定金，非预付款。
- **提货款**：含"发货前支付 / 设备到货前 X 天支付"（例："设备到货前15天支付该批次电梯供应金额的20%"）→ 提货款，非预付款。
- **货到工地**：关键词"交货验收 / 开箱验收"。**例外**：最终限制条件为"货到工地"时，即使涉及国债补贴仍归此类（例："货到工地支付70%,若国债未发给则由代理垫付"）。
- **工程整体竣工**：涉及"专项维修基金 / 政府补贴 / 国债补贴 / 审计审价完成"（除非触发"货到工地"例外）。
  - 例："政府验收完毕取得国债补贴及维修基金审批完毕后支付100%安装款" → 工程整体竣工。
- **当地政府部门验收后**：关键词"政府验收 / 特种设备验收 / 技监部门验收 / 安装验收合格"；"进度款"若上下文为安装验收阶段亦归此类（例："进度款：再向乙方支付进度款,即总价10%的合同款"）。
- **结算完成**：**严格条件**——`payment_clause` 原文必须含"审定总价 / 审价 / 审计 / 移交款 / 办理结算手续"等**结算专属词汇**（例："通过审价后…支付审定总价扣除已支付款项后的余款"）。
  - ❌ 仅含"验收通过后支付剩余款项 / 验收合格后支付余款"等表述，**不**归此类——这只是"验收 + 余款"的常见付款节点表达，应保留输入节点的原始 `payment_type`（通常是 `当地政府部门验收后` 或 `工程整体竣工`），**禁止**改判为 `结算完成`。
- **电梯移交用户后**：关键词"移交物业 / 移交钥匙 / 移交手续 / 甲方接收 / 移交甲方 / 视为自动移交 / 移交用户 / 交付使用"。
  - **优先级凌驾于"政府部门验收/技监验收"之上**：当条款同时含"移交手续 / 甲方接收 / 移交甲方"等"移交"动作语义和"政府部门验收合格"等验收措辞时，**优先归 电梯移交用户后**——验收只是前置条件，真正的支付触发是"移交+接收"。
  - 例："电梯安装结束并通过政府部门验收合格颁发安全检验合格证后，乙方与甲方办理书面移交手续且甲方接收后…甲方在21天内向乙方支付设备合同总金额的30%。若因甲方原因不能完成移交则视为自动移交甲方接收" → **电梯移交用户后**（不是 当地政府部门验收后；不是 结算完成）。

### Step 6 | 最终校验
- `final_ratio` 之和应为 100%；微小误差在**最后一个**节点微调。
- 过滤 `final_ratio ∈ {{0, null}}` 的节点。
- 过滤 `final_amount ∈ {{0, "0", null 之外的 0 值}}` 的节点。

## 4. 输入数据
- `{batch_payment_items}`：待处理节点列表。
- `{chunk_text_map}`：条款上下文，**仅用于理解判断，禁止引用内容**。
- `{rag_examples}`：参考示例，**仅用于理解任务/格式，禁止引用其内容**。

## 5. 输出格式（严格遵守）
整个输出是**一个** JSON 代码块 ` ```json ... ``` `，代码块内为 JSON 数组；数组元素依次为：**N 个付款节点对象 + 1 个 thinking_output 对象**。

**付款节点对象严格只含 5 个字段**（多一个、少一个均视为违规）：
```json
[
  {{
    "id": "输入的原始id",
    "payment_clause": "原文逐字复制",
    "payment_type": "13 种标准类型之一",
    "final_ratio": "如 30.0%；无则 null",
    "final_amount": "金额字符串；无则 null"
  }},
  {{
    "thinking_output": "纯文本思考过程：Step1过滤→Step2计算→Step3金额→Step4合并→Step5分类→Step6校验"
  }}
]
```

## 6. JSON 语法硬约束（违反将导致解析失败）
1. **禁止悬挂逗号**：任何 `}}` 或 `]` 前一个非空白字符不得是 `,`。
   - 错误：`{{ "a": 1, }}` 、`[ {{...}}, ]`
2. `thinking_output` **仅**以单个对象形式出现在**数组最后位置**；禁止拆多份、禁止散落 `Step X:` 裸文本进数组。
3. 数组中除 `thinking_output` 对象外，**禁止**出现任何"非条款对象"（说明/元信息对象）。
4. 字符串内换行必须转义为 `\\n`，禁止裸换行。
5. **输出前自检**：以上 4 条逐条确认 → 再输出。
"""



INSTALL_PAYMENT_SUMMARY_RATIO_PROMPT = """# 安装付款条款批量复核

## 角色
对批量"安装付款"节点做**轻量校验 + 格式标准化**。**不重做抽取、不重新分类、不改写原文**。

## 最高铁律（违反即任务失败）
1. **payment_clause 逐字 passthrough**：必须**一字不差**等于输入；禁止改写、补充、截断、用其他条款拼接。
2. **字段默认 passthrough**：`payment_type` / `final_ratio` / `final_amount` 默认**原样输出**输入值；仅在 Step 3 明确许可的情形下可修正。
3. **禁止外部引用**：`chunk_text_map` / `rag_examples` 仅辅助理解，**严禁**把它们的内容作为字段值输出，**严禁**用它们改写任何字段。
4. **id 不变、不增不减**（仅 Step 1 过滤、Step 2 质保金合并允许减少 id）。
5. **`thinking_output` 仅一个，放数组最末**。

## Step 1 | 0 元一票过滤（硬规则）
若 `payment_clause` 含 `0元 / 零元 / RMB0 / 零元整` → **整条丢弃**，不参与后续。
（其它过滤维度（保函/空泛付清/拒付等）已在上游"条款有效性校验"完成，本阶段**不再二次过滤**。）

## Step 2 | 质保金合并（硬规则）
若批次中存在多条 `payment_type = 质保金`：
- 合并为 1 条：`final_ratio` 累加（如 2×1.5% → 3%）、`final_amount` 累加（皆 null 则保 null）、`payment_clause` 用 `\\n` 拼接、保留首个 `id`。

## Step 3 | 字段校验与允许修正

校验仅以下 3 类情形可修改字段，**其它一律 passthrough**。

### 3.1 payment_type
- 输入 `payment_type` ∈ 下方"12 类标准节点" → **直接 passthrough**，**禁止**改成其它类型。
- 输入 `payment_type` 不在 12 类（拼写错/英文/空/null） → 允许修正为最接近的标准节点；**修正依据只能来自该节点自身的 `payment_clause` 原文**，禁止参考 chunk_text_map / rag_examples。
- ❌ **严禁**：因 chunk 里出现别的付款描述、或某 type 在批次中重复，就把 in_X 的 type 改成另一类。Per-clause 已识别的 `payment_type` 是权威。

**12 类标准节点**：
1.定金 | 2.进场前（首付） | 3.进场后 | 4.移交前 | 5.报验前 | 6.公司验收后 | 7.当地政府部门验收后 | 8.电梯移交用户后 | 9.工程整体竣工 | 10.特殊付款-移交前 | 11.特殊付款-移交后 | 12.质保金

### 3.2 final_ratio
- 合法百分比字符串（如 `"30%"` / `"30.0%"`）→ passthrough。
- 裸小数（如 `0.3`）→ 修正为 `"30%"`。
- null 且 `final_amount` 有效 → **保留 null**（合法情形）。
- **求和校验**：仅当**所有保留节点的 final_ratio 均非 null** 时，要求和 ≤ 100%，<1% 误差在最后一个节点微调；若存在任一 null，**跳过求和校验**。**严禁**为了凑 100% 凭空填充 ratio。

### 3.3 final_amount
- 合法金额字符串 / 数字 → passthrough（统一转字符串）。
- 误填为百分比 / 小数比例 → 修正为 null。
- null → 保留 null。

## Step 4 | 输入数据
- `{batch_payment_items}`：待校验条款列表。
- `{chunk_text_map}`：上下文，**仅辅助理解，严禁引用其内容到输出字段**。
- `{rag_examples}`：示例，**仅辅助理解格式，严禁引用其内容到输出字段**。

## Step 5 | 输出格式（严格遵守）
整个输出是**一个** JSON 代码块 ` ```json ... ``` `，代码块内为 JSON 数组：N 个付款节点对象 + 1 个 thinking_output 对象。

付款节点对象**严格只含 5 个字段**：
```json
[
  {{
    "id": "输入原始id",
    "payment_clause": "逐字复制输入（禁止改一个字）",
    "payment_type": "12 类标准节点之一",
    "final_ratio": "30%" 或 null,
    "final_amount": "金额字符串" 或 null
  }},
  {{
    "thinking_output": "Step1过滤X条 / Step2合并质保金Y条 / Step3字段校验通过"
  }}
]
```

## JSON 语法硬约束
1. 禁止悬挂逗号（`}}` / `]` 前不得有 `,`）。
2. 字符串内换行必须转义为 `\\n`，禁止裸换行。
3. 数组中除最后一个 `thinking_output` 对象外，禁止出现任何非条款对象。
4. 输出前自检以上 3 条 → 再输出。
"""

WARRANTY_SUMMARY="""# 角色
你是一个严格遵守规则的合同条款分析专家。你的唯一任务是对输入的质保期条款进行分析，并按照指定格式最终输出最正确的质保期信息。

# 核心业务规则
1.  **对象锁定**: 只提取**整机/主设备**的质保信息。忽略“易损件”、“配件”、“电池”等附属部件。
2.  **优先提取逻辑**:
    *   当条款包含双重期限（如“A事件起X个月，但最迟不超过B事件起Y个月”）时：
    *   `warranty` = **X** (优先提取生效后的时长)。
    *   `closed_end_conditions` = **B事件起Y个月** (闭环/截止描述)。
3.  **单位强制转换**:
    *   所有时长必须统一转换为 **“个月”**。
    *   例如：“1年” -> “12个月”，“2年” -> “24个月”， “5年” -> “60个月”。

# 绝对输出规则
- **绝对规则1: Markdown代码块**: 你的**整个输出**必须严格地包裹在一个JSON代码块中，像这样: ` ```json ... ``` `
- **绝对规则2: 必须是数组**: 代码块内部的根结构**必须是**一个JSON数组。它**必须**以 `[` 开头，并以 `]` 结尾。
- **绝对规则3: 数组内容**: 这个数组**必须**包含：
    1.  一个或多个代表**质保信息**的JSON对象（通常为1个）。
    2.  **数组的最后一个元素**，**必须是**一个包含`"thinking_output"`键的JSON对象。

**最终输出结构必须严格如下所示:**
```json
[
  {{
    "warranty": "24个月",  // 必须以'个月'结尾
    "effective_conditions": "质保期生效/起算条件描述",
    "closed_end_conditions": "闭环/截止条件描述(若无则为空字符串)"
  }},
  {{
    "thinking_output": "纯文本格式的思考过程，解释为何提取该时长以及如何进行的单位换算"
  }}
]
```

# 参考示例
## 示例条款1
Input: "9.3由卖方或卖方委托方负责安装的产品，在买方和使用管理方遵守安全技术规范合理正常的保管和使用下，产品质量保修期（质保期）为合同设备经当地特种设备管理部门监督检验合格之日起的36个月，但最迟不超过本合同第五条确认出货日期起的42个月。"
Output:
```json
[
  {{
    "warranty": "36个月",
    "effective_conditions": "合同设备经当地特种设备管理部门监督检验合格之日",
    "closed_end_conditions": "最迟不超过本合同第五条确认出货日期起的42个月"
  }},
  {{
    "thinking_output": "1. 识别双重期限：'检验合格起36个月'(优先) vs '出货起42个月'(闭环)。2. 提取warranty：取优先项'36个月'。3. 字段映射：'检验合格...'填入生效条件，'最迟不超过...'填入闭环条件。"
  }}
]
```
## 示例条款2
Input: "- 5)保修期期限：设备安装竣工验收合格后免费维修保养2年，产品质量保修2年 - 6)免费质保期期限起计方式：从最终验收合格并由质监站和技术监督局签发《电梯准用证》和《电梯安全准用证》交付买方使用之日起计算"
Output:
```json
[
  {{
    "warranty": "24个月",
    "effective_conditions": "从最终验收合格并由质监站和技术监督局签发《电梯准用证》和《电梯安全准用证》交付买方使用之日起计算",
    "closed_end_conditions": ""
  }},
  {{
    "thinking_output": "1. 识别时长：原文'2年'。2. 单位换算：2 * 12 = '24个月'。3. 识别条件：结合条款5和条款6，起算点为条款6描述的完整内容。4. 闭环判断：无'最迟不超过'字样，闭环条件留空。"
  }}
]
```

# Input Data
- `{warranty_clauses}`：质保期条款。
"""

RESULT_VERIFICATION_PROMPT="""# 付款条款去重

## 任务
对输入条款按 payment_type 去重。**相同 clause_category + payment_type 的多个条款，只保留最合适的一个，丢弃其余。**

## 去重选择规则（按优先级）
1. 优先保留包含具体支付动作（"支付""付款"）且有金额/比例的条款
2. 优先保留条款文本更长、信息更完整的
3. 过滤0元条款（含"0元""零元"或金额为0）

## 输入
- `validation_equipment`: {validation_equipment}
- `validation_install`: {validation_install}

## 输出要求
只输出JSON数组，无其他文字。每个 payment_type 只输出一个ID。
```json
[{{"select_clause_id":"保留的ID"}},{{"thinking_output":"去重理由"}}]
```

## 示例
输入：id="0_3" payment_type="验收后" clause="验收后三天内支付18465元" 和 id="0_4" payment_type="验收后" clause="分批次验收分批支付余额"
输出：[{{"select_clause_id":"0_3"}},{{"thinking_output":"0_3含具体金额和时间节点，信息更完整，丢弃0_4"}}]

错误示例：同时输出0_3和0_4 → 错误！相同payment_type只能保留一个。

输出："""

PAYMENT_CLAUSE_VALIDATION_PROMPT = """# 任务
判断"待验证条款"本身是否为有效付款条款。
有效付款条款必须**同时包含**：①正向支付动作（"支付""付款""付清""完成款项支付"等）+ ②具体金额或比例（X万元、X%等数值）。
两者缺一则 is_valid=false。

若"待验证条款"**主体**同时包含【正向支付动作（支付/付款/付清/汇入/完成款项支付）】+【具体金额或比例（X%/X万元/X元）】，
则一律 is_valid=true，**不再**进入下方"直接排除"列表的任何判断。
附属位置（末尾从句、质保金清算句、违约附带句等）出现的"扣除/划扣/扣减/拒付/暂停/抵扣/冲抵"等词**一律视为附属条件**，不构成排除依据。
⚠️ **质保金扣留/提留视为正向支付动作**：条款主体描述"提留/扣留/扣作/留作/作为质量保证金/质保金"+ 具体比例或金额（如"剩余设备总价的5%即¥235380元由买方一次性提留作为质量保证金"），属于质保金节点的现金扣留支付，**视为有效付款条款**（is_valid=true），不因缺少"支付"二字而排除。
  ⚠️ **保函例外**：若条款同时含"保函/担保函"（如"作为质量保证金保函"），属凭证非现金扣留，**不适用**本规则，须进入"直接排除"列表判断。
⚠️ **"节点名：比例"简写格式视为正向支付动作**：条款形如"预付款：合同设备款15%"/"提货款：95%"/"尾款：5%"等"付款节点名+冒号+比例/金额"结构，冒号即隐含支付动作，**视为有效付款条款**（is_valid=true），不因缺少"支付"二字而排除。
⚠️ **赔偿性支付例外**：若"支付X%/X元"出现在违约/赔偿后果语境中（典型结构："承担…经济损失/法律责任…同时/并向甲方支付X%"），该"支付"属于罚款/赔偿性质，**不适用**本强制通过规则，须进入"直接排除"列表判断。
⚠️ **发票/单据开具例外**：若"支付X款项"仅作为时间/条件引用（典型结构："收到买方支付的全额款项后，向买方提供发票"），条款主旨是发票/单据的开具或提供，该"支付"非付款指令，**不适用**本强制通过规则，须进入"直接排除"列表判断。

## 特殊有效模式（命中则 is_valid=true，即使无显式金额/比例）
- **隐含全额支付**：条款明确表述"无预付款/无进度款"或"一次性付款"，且描述了验收/审核后完成全部款项支付的流程。此类条款隐含比例=100%，视为有效付款条款。
  - 示例1："付款条件为无预付款和期间进度款，乙方在供货、安装调试完成及验收合格后…完成款项支付" → true
  - 示例2："无预付款和期间进度款，乙方供货、安装调试验收合格，视国债/专项资金到位情况…财务审核通过后10个工作日内完成款项支付" → true（"视资金到位情况"是付款时序条件，不影响有效性）
  ⚠️ 前置推理若需说明，请使用格式 `描述 → true` 或 `描述 → false`（禁止写 `→ is_valid=true/false`）

## 直接排除（**仅命中以下列表中的明确条目**才能 is_valid=false；严禁自创排除理由如"扣除/划扣/扣减/有权扣款/抵扣/冲抵"等——这些都不构成排除项）
- 含"违约金/罚款/赔偿/赔偿损失/违约责任/经济损失"，且**条款主旨是处罚/赔偿**（而非附带条件）。
  - 示例："乙方承担因此产生的全部经济损失及法律责任，同时向甲方支付合同总额 20%" → false（整条款主旨为发票违规的赔偿后果，非付款节点）
- 含"逾期"且含"利息/利率/滞纳金"
- **条款主旨是描述质量问题处理流程**（如"出现质量问题时如何索赔/退换/划扣"）。
  ⚠️ 注意：若条款主体描述了支付动作（如"支付质保金5%"），仅在末尾或附属位置提及"质量问题/扣除/划扣/扣减/抵扣/冲抵/有权扣款"等作为支付的附属条件，**不应排除**——这是有效付款条款。
- 含"有权不支付/拒付/暂停支付/有权暂缓支付"，且**条款中无其他正向支付动作+具体金额**（注意："有权划扣/有权扣减"**不属于**此项；若条款主体含"支付X%"等独立付款节点，仅在末尾附属位置提及"有权暂缓支付"作为保函/质保的惩罚条件，**不应排除**——这是有效付款条款）
- 含"保函/担保函"但无"支付X%/X元"的实际付款
- 仅描述价格/报价/总价/单价，无支付动作
- 仅描述支付方式/工具，无金额比例
- 仅描述付款前置条件/依据/验收要求，无支付动作和金额
- 仅描述供货期/交货期/工期等时间安排，无支付动作
- 概述性/引导性条款，无具体金额**且不符合上述"隐含全额支付"模式**
- 指引条款（如"详见第X条"）
- 单据要求（如"正本X份，副本X份"）
- **发票开具/提供条款**：条款主旨是发票的开具、提供、出具（如"提供发票""开具发票""出具发票"），其中"支付款项"仅作为时间/条件前置引用（如"收到…支付的全额款项后，向…提供发票"），并非付款节点。
- 附件/目录/表格标题
- 不完整的句子片段
- **引用性兜底条款**：条款中出现了"按上述/按前述/按第一条/应按上述方式/应按前述规定/应按本条/依照前款"等引用性表述，且条款自身**不含独立的支付比例或金额数值**（注意：若条款中同时包含独立的比例/金额，如"按上述方式支付50%"，则**不应排除**）
- **模板留空条款**：条款中的金额或比例字段为空白/未填写，表现为 `/%`、`的%`、`_%`、`□%`、`__元`、`___万` 等占位符号（`%` 前无具体数字），属于未填写的合同模板，is_valid=false。
  ⚠️ 示例："支付合同总价的/%" / "付至结算价款的%" / "付至安装总价（含税）%" / "付至货物总价的_%" → is_valid=false（无具体数值）

## 输出
只输出JSON对象，无其他文字。
⚠️ **id 必须与输入的id完全一致**，不得臆造、续写或引用其他条款的id。
⚠️ 判断时以条款原文内容为准，`clause_class` 仅作参考标签，**不作为判断依据**——即使 clause_class 标注为"质保金"，若条款原文描述的是"竣工款/进度款"等正向支付节点且含具体比例，仍应判 is_valid=true。
{{"id": "输入的id", "is_valid": true/false, "reason": "简短理由"}}

## 待验证条款
{validation_clauses}
"""


# ===== 混签付款条款归属判定（仅对 clause_class 含 混签付款条款 的条款触发） =====
PAYMENT_CLAUSE_CATEGORY_PROMPT = """# 混签付款条款归属判定
## 任务
判断下方条款归属：`equipment_payment` / `installation_payment` / `both`（三选一）。

## ⚠️ 输出铁律（先读，后分析）
- **直接输出 JSON，禁止输出任何分析/思考文本**。
- JSON 固定 3 字段：`id`（原样复制输入值）/ `category`（三选一）/ `reason`（≤20字，说明主体词）。
- 找到主体词后**立即输出 JSON，不再检查其他条件**。

```json
{{"id": "0", "category": "both", "reason": "主体词为结算总价（合计总价）"}}
```

---

## 判定方法：找付款主体词

**从 clause 原文找"支付 [主体] 的 N%" 或 "支付至 [主体] 的 N%"中的 [主体]，查表得出结果，立即输出 JSON。**

| 主体词 | 结果 |
|---|---|
| 设备总价 / 设备款 / 设备价款 / 设备费 / 设备部分 / **所交付货物总价款** / **货物总价款** / **货物价款** | `equipment_payment`（⚠️ 设备总价 = **仅设备部分**，即使在混签合同中也只判 equipment，**不得**判 both） |
| 安装款 / 安装费 / 安装工程费 / 施工费 / 安装价款 / **安装合同总价款** / **安装工程总价** | `installation_payment` |
| 合同总价 / 合同价款 / 合同总金额 / **结算总价** / 结算金额 / 合同结算金额 / 审定总价 / 产品总价 / 工程总价 / 项目总价 / 总金额 / 尾款总金额 | `both` |
| 同一条款同时含设备主体词 AND 安装主体词 | `both` |
| 主体词无法识别 | `both`（兜底） |

---

## 关键规则（避免常见误判）
0. **标题优先于一切**：若 clause 以「设备采购」「设备款支付」「设备付款」「设备费用支付」等**设备标题**开头 → 整段判为 `equipment_payment`，**即使出现"合同结算金额""结算总价""合同总价"等 both 主体词也不判 both**（这些词在设备标题下指设备子合同金额）。
   若 clause 以「安装费支付」「安装款支付」「安装付款」等**安装标题**开头 → 整段判为 `installation_payment`，同理。
   示例："设备采购：…(3)支付至合同结算金额的100%…(4)结算额5%质保金保函" → `equipment_payment`（标题限定，"合同结算金额"是设备合同结算额，不是混签总价）。
1. **计算基数 ≠ 付款主体**：若"合同总价/结算总价"出现在"相当于/等于/计算为 合同总价N%**的[主体名词]**"结构中，真正的付款主体是"的"后面的名词，而非"合同总价"。
   示例："支付相当于合同总价50%的**安装款**" → 主体是"安装款" → `installation_payment`（不是 `both`）。
2. **触发条件 ≠ 主体**：「安装调试验收合格后支付至结算总价的97%」→ 主体是"结算总价" → **both**，触发条件"安装验收"**不影响结果**。
3. **节点名 ≠ 主体**：「支付合同总价的10%作为预付款」→ 主体是"合同总价" → **both**，"预付款"是节点名。
4. **保函/发票忽略**：「结算额5%的质保金保函」/「开具设备款发票」不是付款动作，不影响主体判定。
5. **质保金看主体**：「剩余安装款的5%作为质量保证金」→ 主体"安装款" → `installation_payment`。
6. **货物主体词覆盖"合同总价款"**：若同一条款同时含"所交付货物总价款/货物总价款"（货物主体）和"合同总价款"，以货物主体为准 → `equipment_payment`（"合同总价款"在此语境指设备子合同总价，非混签合同总价）。


---

## 示例
- "支付设备总价50%的预付款" → `equipment_payment`（主体：设备总价，仅设备部分，**不判 both**）
- "支付相当于合同总价50%的安装款" → `installation_payment`（"合同总价"是计算基数，真正主体是"安装款"）
- "支付至设备部分的60%" → `equipment_payment`
- "支付合同总价的10%作为预付款" → `both`（主体：合同总价）
- "安装验收合格后支付至结算总价的97%" → `both`（主体：结算总价）
- "支付安装工程费的45%" → `installation_payment`（主体：安装工程费）
- "剩余安装款的5%作为质量保证金" → `installation_payment`（主体：安装款）
- "支付所交付货物总价款的75%作为发货款...支付合同总价款的20%作为验收款" → `equipment_payment`（含"货物总价款"，货物主体覆盖"合同总价款"，规则6）
- "支付安装合同总价款的50%作为安装预付款...支付安装合同总价款的50%作为安装验收款" → `installation_payment`（主体：安装合同总价款）
- "设备采购：(1)设备总价20%…(4)结算额5%质保金保函" → `equipment_payment`（标题限定）
- "设备采购：…(3)支付至合同结算金额的100%…(4)结算额5%质保金保函" → `equipment_payment`（标题限定，"合同结算金额"在设备标题下指设备合同结算额）
- "支付相当于合同总价50%的**安装款**" → `installation_payment`（主体：安装款）
---

## 待判定条款
`{category_clause}`

输出："""

# ===== 单组去重（每次只处理一个 (clause_category, payment_type) 组，单任务单输出） =====
RESULT_VERIFICATION_SINGLE_GROUP_PROMPT = """# 付款条款重复节点处理

## 背景
下方"候选条款"是在同一分类（clause_category）下、**初步识别为相同 payment_type** 的 N 个节点。

---

## 判断流程（按顺序执行，不可跳步）

### 第一步：优先检查是否存在节点类型误判 → action = "correct_type"
**无论比例是否相同，必须先执行此检查。**

仔细阅读每条候选条款的 `payment_clause` 原文，逐条对照当前 `payment_type` 的语义：
- 触发条件（验收节点、阶段事件等）是否真的属于该类型？
- 若某条原文描述的触发事件**与当前 payment_type 明显不符**，说明上游分类出错。

**触发 correct_type 的条件**：至少一条候选条款的原文与当前 payment_type 语义不匹配，应归入另一标准节点。

**操作**：给出正确类型，**全部保留**，不丢弃任何条款。
- 只修正确实有误的条款，其余不动
- `corrected_payment_type` 必须从下方**本组标准节点列表**中选取

---

### 第二步：确认无类型误判后，执行去重 → action = "select_one"
**仅当第一步确认所有候选条款类型均正确时，才执行此步。**

**触发 select_one 的条件**：所有条款均属于同一付款类型且描述的是同一付款事件（真正重复）。

**操作**：从候选中选出最合适的一条，输出其 id；其余丢弃。
选择规则（优先级）：
1. 同时含支付动作（"支付"/"付款"/"付清"/"汇入"）和具体比例/金额的优先
2. 条款文本更长、信息更完整的优先
3. 含"0元""零元"或金额为 0 的不选
4. 难以判断时选第一条（列表首个 id）

---

## 标准节点
`corrected_payment_type` 只能从此列表中选取，禁止使用列表外的类型：
{standard_nodes}

---

## 输出格式
只输出 JSON 对象，无任何其他文字。所有字段均**必须存在**，不可省略。

纠正类型（第一步触发）：
```json
{{"action": "correct_type", "select_clause_id": null, "corrections": [{{"id": "in_2", "corrected_payment_type": "工程整体竣工"}}], "reason": "简短理由（不超过30字）"}}
```

去重保留一条（第二步触发）：
```json
{{"action": "select_one", "select_clause_id": "候选中的ID", "corrections": [], "reason": "简短理由（不超过30字）"}}
```

约束：
- `select_clause_id` 在 select_one 时**必须**是候选条款中存在的 id，禁止新造 id
- `corrections` 中的 `id` 必须是候选条款中存在的 id
- `corrected_payment_type` 必须从上方标准节点列表中选取

---

## 候选条款（待处理输入）
{group_clauses}

输出："""


# ============================================================================
# 付款时效提取（Stage 7）：付款天数 / 最迟付款节点 / 最迟付款时间
# ============================================================================
PAYMENT_TIMING_EXTRACTION_PROMPT = """# 任务
从给定的"付款条款"和"上下文"中精准提取三个时效指标：付款天数、最迟付款节点、最迟付款时间。

## 🚨 第 0 步：强制前置检查（必须最先执行，不可跳过）
在做任何抽取前，先对**"付款条款"本身**做一次**字符级**扫描（**只扫描付款条款，不扫描上下文**）：
- "付款条款"中是否字面包含"最迟"二字（包含"最迟不晚于 / 最迟不超过 / 最迟于 / 最迟在 / 最迟不得晚于"等任意变体）？
- **若"付款条款"中不出现"最迟"二字** → 立即将 `latest_payment_stage` 与 `latest_payment_date` **同时**置为 null，**禁止**再做任何"截止/上限/不晚于"的语义推断；只继续抽取 `payment_days`。
- ⚠️ **特别警告**：上下文（payment_context）里可能出现与付款**无关**的"最迟"——典型如**质保期/保修期**描述"最迟不超过出货日期起的 42 个月"、保函有效期等——这类"最迟"**绝对不能**触发付款时效字段抽取。判定锚点**只看付款条款本身**。
- 注意区分：以下表述**都不算**"最迟"语义，**不得**触发 latest_* 抽取：
  * "X 天前 / X 个工作日前 / 距某日期 X 天前"（这是常规付款时点，不是截止上限）
  * "在……之前 / 不迟于"（无"最迟"前缀的"不迟于"也不算）
  * "应在 / 须在 / 须于 / 应当于"
  * "验收合格后 / 收到发票后 / 经确认后"等普通触发条件
  * 上下文中关于**质保期 / 保修期 / 保函有效期**的"最迟"——与付款时效无关

## 字段定义（严格区分，禁止混淆）
1. **付款天数 (payment_days, int|null)**：常规付款周期，单位"天"。形如"X 天内 / X 个工作日内 / 收到发票后 X 天内 / X 日内"；**严禁**包含"最迟"语义的数字。
   - "X 个工作日" 视为 X 天（不做工作日折算）。
   - ⚠️ **禁止将比例/百分比数字当作天数**：原文中的 `X%`（如 97%、17%、3%）是支付比例，**严禁**提取为 payment_days；只有原文明确出现"X天内 / X工作日内 / X日内"等时间表述才可填入。
   - 若原文无任何付款期限描述 → 返回 `null`。

2. **最迟付款节点 (latest_payment_stage, str|null)**：
   ⚠️ **硬约束（最高优先级）**：
   - **付款条款本身**必须**字面出现"最迟"二字**（如"最迟不晚于""最迟不超过""最迟于""最迟在"），才允许提取该字段。**仅在上下文中出现而条款本身没有 → 直接返回 null**（上下文里的"最迟"可能来自质保期/保函等无关内容）。
   - 输出值必须从以下标准节点列表中选取最匹配的一个：{standard_nodes}
   - 若"最迟"后面关联的是一个**数字时长**（如"最迟不超过30天"）→ 进 latest_payment_date，本字段返回 null。
   - 若"最迟"后面关联的是一个**事件/状态**且能匹配标准节点 → 填入标准节点名称。
   - 若"最迟"后面关联的事件无法匹配任何标准节点 → 返回 null。
   ❌ **严禁**：将普通付款触发条件（如"验收合格后""经确认后20工作日"）当作最迟付款节点——这些只是常规条件，不是"最迟"截止约束。

3. **最迟付款时间 (latest_payment_date, int|null)**：以"最迟不晚于 / 最迟不超过"等语义引出的**截止时长**，单位天。
   - 折算规则："X 个月" → X * 30；"X 周" → X * 7；"X 天" → X。
   - 与"付款天数"互斥：含"最迟"前缀的数字进 latest_payment_date，不进 payment_days。
   - 同样要求**付款条款本身**字面出现"最迟"二字，否则返回 null（仅上下文出现不算）。

## 异常规则
- 上下文未明确提及某指标 → 该字段返回 null。
- **付款条款本身**无"最迟"二字 → latest_payment_stage 和 latest_payment_date 均返回 null（不论上下文是否出现"最迟"）。
- 输出必须是合法 JSON，不得包含任何额外文字、注释或 markdown 代码块。

## 示例
### 示例 1（仅常规周期，无"最迟"关键词 → latest_* 全为 null）
条款：
4.1合同签订后3天内买方支付给卖方设备总价5%作为定金
上下文：
## 四、付款方式和付款条件
4.1合同签订后3天内买方支付给卖方设备总价5%作为定金。
4.2买方依据确定的最终交货日期办理预约发货前15天，买方按本合同约定的卖方账户支付全部货款余额。
输出：{{"payment_days": 3, "latest_payment_stage": null, "latest_payment_date": null}}

### 示例 2（常规 + "最迟"关联天数 → latest_payment_date 填入，latest_payment_stage=null）
条款：
2.2.安装验收款：安装人员进场后 7 个工作日内，甲方支付安装费的 30%，但最迟不超过到货后30天。
上下文：
2.2.安装验收款：...最迟不超过到货后30天。2.3.验收款：...
输出：{{"payment_days": 7, "latest_payment_stage": null, "latest_payment_date": 30}}

### 示例 3（"最迟"同时关联事件节点 + 天数 → 两者都填）
条款：
4 设备安装、调试完成，在验收合格之日起15天内办理结算并支付余款。买方最迟不得晚于货到现场后的6个月内付清全部合同价款。
上下文：
4 ...验收合格之日起15天内... 买方最迟不得晚于货到现场后的6个月内付清全部的合同价款。
输出：{{"payment_days": 15, "latest_payment_stage": "货到工地", "latest_payment_date": 180}}

### 示例 4（有"验收后20工作日"但无"最迟"关键词 → latest_* 全为 null）
条款：
(2)当设备安装、调试完毕，收到质量技术监督局签署的验收报告，经业主确认后20个工作日内，业主支付合同金额的30%。
上下文：
(2)当设备安装、调试完毕...经业主确认后20个工作日内，业主支付合同金额的30%。
输出：{{"payment_days": 20, "latest_payment_stage": null, "latest_payment_date": null}}

### 示例 5（含比例数字但无付款天数 → payment_days=null，禁止将97/17/3当天数）
条款：
(2)综合备案验收通过、档案资料移交且竣工结算审计结束并经甲方确认后，支付至结算价的97%，3%余款作为质量保修金。
上下文：（略）
输出：{{"payment_days": null, "latest_payment_stage": null, "latest_payment_date": null}}

## 当前条款类别
{clause_category}

## 标准节点列表（latest_payment_stage 仅允许从中选取）
{standard_nodes}

## 待提取
条款：
{payment_clause}
上下文：
{payment_context}

## 输出
只输出一行 JSON，无 markdown、无解释文字：
{{"payment_days": <int|null>, "latest_payment_stage": <str|null>, "latest_payment_date": <int|null>}}
"""



# =============================================================================
# 合同总金额提取（用于 /compare_contract_price 接口）
# =============================================================================
# 单大括号占位符，调用方使用 str.format() 注入：
#   - {contract_price_clause}         合同总价条款原文（必填）
#   - {contract_price_clause_context} 条款上下文（可空，调用方传入空串即可）
CONTRACT_PRICE_EXTRACTION_PROMPT = """你是合同金额抽取专家。请从下方"合同总价条款"中精确抽取**合同总金额**（单位：元，人民币）。

## 抽取规则
1. 仅返回一个数值；优先采用阿拉伯数字金额，若仅有大写中文金额则换算为阿拉伯数字。
2. 单位换算：「万元」乘以 10000，「亿元」乘以 100000000；带「元/RMB/￥/¥/人民币」前后缀的统一去除。
3. 含税总价 / 合同总金额 / 合同总价款 / 价款总额 优先于分项金额。
4. 若条款中只有比例、单价或分项金额（无法得到合同总金额）→ 返回 null。
5. 严禁推理、估算或借助上下文之外的信息；上下文仅辅助理解条款，不得引用其中的数字推断总价。

## 输出格式（必须严格遵守）
- 仅输出一行合法 JSON，前后**禁止**任何文字、解释、Markdown、代码块、思考过程。
- `contract_price` 必须为**纯数字**（int 或 float）；**禁止字符串、禁止千分位逗号、禁止单位/货币前缀**；无法抽取时为 `null`。
- 合法示例：`{{"contract_price": 4308000}}` 或 `{{"contract_price": null}}`
- 非法示例（绝对不要这样输出）：`{{"contract_price": "4308000"}}`、`{{"contract_price": "4,308,000"}}`、`{{"contract_price": "￥4308000"}}`

## 输入
合同总价条款：
{contract_price_clause}

条款上下文（可能为空）：
{contract_price_clause_context}
"""


# =============================================================================
# 比例求和矫正（Chain 11/12）
# =============================================================================
# 单大括号占位符，调用方使用 PromptTemplate.format() 注入：
#   - {batch_items}     当前组所有节点 JSON 字符串（必填）
#   - {merged_context}  合并后的合同上下文文本（必填）
#   - {actual_sum}      当前比例和百分比，如 "85.0"（必填）

RATIO_CORRECTION_UNDER_PROMPT = """# 付款比例不足 100% 矫正

## 任务
1. 仔细阅读合同上下文，找出可能遗漏的付款节点。
2. 若发现遗漏节点，补充为新节点（id 设为 null，action 设为 "new"，需提供 payment_type、final_ratio、payment_clause）。
3. 若现有节点比例偏低，根据上下文修正 final_ratio（action 设为 "corrected"）。
4. 无需修改的节点 action 设为 "kept"，final_ratio 保持原值。
5. 若上下文中存在连续付款条款链（如 4.1、4.2、4.3），必须优先从该连续条款链中识别遗漏节点并补全比例，而不是从远处章节的相似条款中抽取节点。
6. 必须结合 payment_clause 与 merged_context 识别条款编号、标题词和相邻条款关系，优先沿连续主付款标题链补全，避免被远处章节的重复标题误导。
7. 最终所有保留节点的 final_ratio 之和**必须等于 100%**。
8. 微小误差（<1%）在最后一个节点微调即可。

## 输出格式（JSON，必须严格遵守）
- action 取值：kept / corrected / new / removed
- final_ratio 为百分比字符串，如 "30%"
- 新增节点必须提供 payment_type 和 payment_clause
{{"items": [{{"id": "eq_0", "payment_type": "预付款", "final_ratio": "30%", "action": "kept"}}, {{"id": null, "payment_type": "质保金", "final_ratio": "5%", "payment_clause": "...", "action": "new"}}]}}

## 背景
当前{category_desc}付款节点的比例总和仅为 {actual_sum}%，**不足 100%**。
说明可能存在遗漏的付款节点，或部分节点比例被低估。

## 当前节点列表
```json
{batch_items}
```

## 合同上下文
```
{merged_context}
```
"""

RATIO_CORRECTION_OVER_PROMPT = """# 付款比例超出 100% 矫正

## 任务
1. 检查是否存在重复/冗余节点，若有则标记 action="removed"（final_ratio 可保留原值或设为 null）。
2. 检查是否有节点比例被高估，根据上下文修正 final_ratio（action 设为 "corrected"）。
3. 无需修改的节点 action 设为 "kept"，final_ratio 保持原值。
4. 最终所有保留节点（非 removed）的 final_ratio 之和**必须等于 100%**。
5. 微小误差（<1%）在最后一个节点微调即可。
6. **严禁**为了凑 100% 凭空填充 ratio 或凭空新增节点。

## 重复节点识别策略（重要）
1. **最高优先级规则：先计算超额值**。先计算超额比例 `overflow = 当前总和 - 100%`。若存在某节点的 `final_ratio` 与 `overflow` **完全一致**，默认必须优先移除该节点；若移除后总和正好回到 `100%`，则该方案应直接视为默认最优方案。
2. **精确匹配优先于其他弱信号**：只要存在与超额值完全一致的节点，条款文本相似度、累计付款提示、章节位置、标题词、相邻条款支撑等都只能作为次级辅助判断，不能覆盖这条最高优先级规则。
3. **受限例外条件**：只有当条款文本与上下文能够**明确证明**该精确匹配节点不是冗余项、且另一个节点才是重复或错误节点时，才允许不采用默认移除方案。模糊倾向、弱语义联想或不充分线索都不足以推翻精确匹配规则。
4. **条款文本比对（次级判断）**：仔细比对各节点的 payment_clause，若两个节点描述同一付款事件（如均含"工程竣工款""政府验收""审价报告"等相同关键表述），则它们是重复节点。
5. **累计付款提示（次级判断）**：条款中可能包含"付款至审定后总价的X%"等累计提示。若两节点的累计提示指向不同的累计比例（如一个"付至40%"、一个"付至95%"），说明它们是同一付款阶段的不同版本，仅保留与其他节点构成连贯链条的那一个。
6. **标题与章节连续性（次级判断）**：必须结合 payment_clause 与 merged_context 识别条款编号、标题词、相邻条款关系。若两个候选节点含义接近，但一个位于连续主付款条款链（如 4.1、4.2、4.3）中，另一个来自远处章节（如 21.6），则优先保留连续主付款条款链中的节点，优先移除远处章节中的重复或冲突节点。
7. **相邻条款支撑（次级判断）**：若某节点前后均有同一付款章节中的相邻条款支撑，说明它更可能是正式付款节点，不应优先移除。
8. **移除后校验**：在决定移除节点前，先计算移除后剩余节点的比例之和。**必须选择使剩余总和最接近 100% 的移除方案**。如果移除某节点后总和反而更偏离 100%，则不应移除该节点。
9. **说明性示例**：若当前总和为 `110%`，则超额值为 `10%`；若节点中存在 `10%` 节点，应优先将该节点作为移除候选，而不应优先删除 `65%` 这类会让结果远离 `100%` 的节点。

## 输出格式（JSON，必须严格遵守）
- action 取值：kept / corrected / new / removed
- final_ratio 为百分比字符串，如 "30%"
{{"items": [{{"id": "eq_0", "payment_type": "预付款", "final_ratio": "30%", "action": "kept"}}, {{"id": "eq_1", "payment_type": "提货款", "final_ratio": null, "action": "removed"}}]}}

## 背景
当前{category_desc}付款节点的比例总和达到 {actual_sum}%，**超出 100%**。
说明可能存在重复节点、比例判断错误，或不应存在的节点。

## 当前节点列表
```json
{batch_items}
```

## 合同上下文
```
{merged_context}
```
"""


__all__ = [
    "PAYMENT_RATIO_PROMPT",
    "EQUIPMENT_PAYMENT_RATIO_PROMPT",
    "INSTALL_PAYMENT_RATIO_PROMPT",
    "PAYMENT_SUMMARY_RATIO_PROMPT",
    "INSTALL_PAYMENT_SUMMARY_RATIO_PROMPT",
    "WARRANTY_SUMMARY",
    "RESULT_VERIFICATION_PROMPT",
    "RESULT_VERIFICATION_SINGLE_GROUP_PROMPT",
    "PAYMENT_CLAUSE_VALIDATION_PROMPT",
    "PAYMENT_CLAUSE_CATEGORY_PROMPT",
    "PAYMENT_TIMING_EXTRACTION_PROMPT",
    "CONTRACT_PRICE_EXTRACTION_PROMPT",
    "RATIO_CORRECTION_UNDER_PROMPT",
    "RATIO_CORRECTION_OVER_PROMPT",
]
