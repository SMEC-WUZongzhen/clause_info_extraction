_PAYMENT_RATIO_PROMPT_COMMON = """
你是一位顶级的合同支付条款分析专家，任务是精准识别并提取给定条款中的**所有付款节点**（一个条款可能包含多个独立的付款动作）。

## 待分析信息
- **当前支付条款**: {{payment_clause}}
- **条款类型（参考）**: {{payment_type}}
- **前序条款上下文 (用于计算累计比例)**: {{current_clauses_chunk}}

---

## 核心规则

### 1. 多节点识别（最重要）
- **一条条款可能包含多个独立的付款节点**。你必须识别条款中所有独立的付款动作，逐个提取。
- **判断标准**：每个独立的付款动作通常具有各自不同的**触发条件/时间节点** + **支付比例或金额**。
  - *示例*: `"合同生效后支付10%作为首期款；交货前支付60%出货款；验收合格后支付30%验收款"` → 这是**3个**独立付款节点。
- **防止误拆**：对同一个付款动作的补充说明、条件限定、金额与比例的对应关系，**不可拆分为多个节点**。
  - *示例*: `"支付合同总价的30%，即60万元"` → 这是**1个**节点（金额和比例描述同一个支付动作）。
  - *示例*: `"甲方在收到发票后30个工作日内支付合同总价的20%作为预付款"` → 这是**1个**节点（时间条件+比例描述同一个支付动作）。
- **隐含质保金分离（必识别）**：条款出现 `"剩余 X% 作为质保金 / 留作质量保证金 / 作为本工程质量保证金"` 等表述时，即使无独立"支付"动词，也必须**单独提取为一个 payment_type=质保金 的节点**（比例=X%）。
  - *示例*: `"支付工程款至结算审计价的95%，剩余5%作为本工程质量保证金，至电梯验收合格之日起1年后免息支付"` → 这是**2个**节点：结算节点 65%（=95%-前序30%）+ 质保金 5%。

### 2. 提取目标（对每个节点）
- **payment_type**: 根据条款语义判断付款类型，**必须从下方标准节点列表中选择最匹配的类型**。
  - **标准节点列表**：{standard_node_list}
- **金额 (Amount)**: 提取明确的货币价值，如 "50万元"、"RMB1,000,000"。
    若支付条款中没有明确的金额，查看上下文，若从上下文有明确的金额，则提取上下文的金额。
- **比例 (Ratio)**: 提取单次应付的百分比。

### 3. 比例计算原则
- **直接单次比例**: 条款明确表述 "支付X%"、"总价的X%" 时，直接使用。
  - *示例*: `"支付该批次设备总价的60%"` → `60%`
- **累计比例转换**: 条款表述 "支付至X%"、"累计支付达X%" 时，通过 `目标累计比例 - 前序累计比例` 计算当前单次比例。
  - *示例*: 前序已付70%，当前为 `"支付至85%"` → 当前单次比例为 `15%`
- **隐含剩余支付**: 条款表述 "支付余款"、"结清尾款"、"付清全部合同价款" 时，通过 `100% - 前序累计比例` 计算差额。

### 4. 金额与比例关系
- 如果某节点**同时包含金额和比例**（如 "支付合同总价的30%，即60万元"），则两者都提取。
- 如果**仅包含金额**（如 "支付定金50,000元"），则金额被提取，比例输出 `null`。
- 如果**仅包含比例**（如 "支付合同总价的30%"），则比例被提取，金额输出 `null`。

### 5. 必须排除的无效条款
- **标题类**: 任何章节标题（如 "## 四、付款方式"、"第X条 付款"）必须输出 `null`。
- **保函类**: 任何涉及银行保函、履约保函、质保保函的条款必须输出 `null`。现金形式的质保金返还除外。
  - *示例*: `"提供金额为合同价5%的质量保函"` → `null`
- **程序性条款**: 仅描述付款流程、时间、发票、运费、结算手续等，不涉及实际支付动作的必须输出 `null`。
  - *示例*: `"运费由卖方代收"`、`"开具等额增值税专用发票"` → `null`
- **重复性/非支付条款**: 仅重申质保期、验收流程等，无新的支付信息的必须输出 `null`。
- **仅描述支付方式/工具**: 条款仅说明以何种方式支付（如商业承兑汇票、银行转账、信用证、电汇），不包含任何金额或比例的，必须输出 `null`。
  - *示例*: `"款项支付均采用为期6个月或者12个月的商业承兑汇票。买方承担贴息费用。"` → `null`（仅描述支付工具，无金额比例）
- **概述性/引导性条款**: 仅描述付款安排方式（如"分期分批供货"、"按进度付款"、"按每批次比例分批支付"），但不包含任何具体金额、比例或明确单次支付动作的，必须输出 `null`。
  - *示例*: `"本合同电梯设备款项根据工程建设进度，分期分批供货、付款、结算"` → `null`（仅为付款条款的引导语/前言，本身无实质支付内容）
  - *示例*: `"产品如分批次验收，委托方按每批次比例分批支付施工费余额"` → `null`（仅描述分批支付的分摊方式，无具体比例/金额，依附于其他条款）
- **付款凭证/通知类程序条款**: 主要内容为"付款后将付款凭证传真/寄送""注明付款单位名称、合同编号""以银行付款凭证为准"等付款后的手续、通知、凭证处理，即使文本含"支付"字样，也必须输出 `null`。
  - *示例*: `"委托方应在付款后，将付款凭证传真或寄送至受托方的合同经办人，并注明付款单位名称、合同编号"` → `null`（纯凭证处理程序）
- **残缺/截断片段**: 条款文本明显不完整，缺乏完整的主谓宾结构，语义不明确的，必须输出 `null`。
  - *示例*: `"求的，甲方有权不支付款项。"` → `null`（残缺片段，无法判断支付内容）
- **否定/拒付类条款**: 表述"不支付"、"有权拒付"、"暂停支付"等拒绝或限制支付情形的，必须输出 `null`。

---

## 分析步骤
1.  **条款性质判断**: 首先判断当前条款是否为必须排除的无效条款（标题、保函、纯程序性）。如果是，立即停止并输出 `null`。
2.  **付款节点识别**: 在有效条款中，识别条款包含**几个独立的付款动作**。每个付款动作应有独立的触发条件和支付比例/金额。
3.  **逐节点提取**: 对每个识别出的付款节点，分别提取 `payment_type`、`ratio`、`amount`。
4.  **比例计算**: 对每个节点判断比例是"直接单次"还是"累计"类型，并做相应计算。
5.  **格式化输出**: 根据提取结果，严格按照下方的JSON格式进行输出。

---

## RAG参考示例
{{rag_examples}}

## ⚠️ 重要提醒
- **RAG示例仅供参考分析模式，绝不可直接使用其数值**。所有提取结果必须严格来源于 `{{payment_clause}}` 的原文。
- **严格遵守排除规则**，避免将保函、程序性描述误判为实际支付。
- **精准识别**：
  - ✅ **支付**: `"支付30%货款"`、`"支付余款"`、`"支付50万元"`
  - ❌ **非支付**: `"提供5%的保函"`、`"开具发票后付款"`、`"质保期为12个月"`、`"采用商业承兑汇票"`

## 节点判断注意事项 
{judgement}
---

## 输出格式要求
严格输出JSON数组，**不要包含任何解释性文字**。数组中每个元素代表一个独立的付款节点。

{output_examples}

每个节点对象的字段说明：
- **payment_type**: 必须从上述标准节点列表中选择。
- **ratio**: 带 `%` 的字符串（如 `"30%"`、`"15.5%"`）。若无比例，输出 `null`。
- **amount**: 数字或字符串（如 `500000` 或 `"50万元"`）。若无金额，输出 `null`。

- 如果整个条款被判定为无效（如标题、保函等），则直接输出 `null`。

请开始分析并提取：```
"""

# ===== 设备付款条款标准节点列表 =====
_EQUIPMENT_STANDARD_NODES = "1.销售定金 | 2.预付款 | 3.提货款 | 4.货到工地 | 5.安装后 | 6.公司验收后 | 7.当地政府部门验收后 | 8.结算完成 | 9.特殊约定付款-移交前 | 10.电梯移交用户后 | 11.特殊约定付款-移交后 | 12.工程整体竣工 | 13.质保金"

_EQUIPMENT_OUTPUT_EXAMPLES = """**单节点条款示例**（仅包含一个付款动作）：
```json
[
  {{"payment_type": "预付款", "ratio": "10%", "amount": null}}
]
```

**多节点条款示例**（包含多个独立付款动作）：
```json
[
  {{"payment_type": "销售定金", "ratio": "10%", "amount": null}},
  {{"payment_type": "提货款", "ratio": "60%", "amount": null}},
  {{"payment_type": "当地政府部门验收后", "ratio": "30%", "amount": null}}
]
```"""
_EQUIPMENT_JUDGEMENT = """
1、`涉及 专项维修基金 政府补贴 国债补贴 等关键词 可能属于 工程整体竣工 节点`
  案例1：“4.2政府验收合格，在政府超长国债等补助下来后，买方三个工作日内支付剩余95%款项，款时间最迟不超过货到工地日期起的10个月。”；存在“政府超长国债”， 属于 工程整体竣工 节点
  ** 货到工地 ** 节点的例外情况, `如下面2个案例：最终限制条件是 货到工地，所以属于 货到工地 节点，而不是 工程整体竣工 节点`
  案例1：“4.3货到工地款：买方在设备国债补贴公示发放后7个工作日内向卖方支付该批次产品总价的75%,最晚不超过货到工地15个月内付清设备款项。”；
  案例2：“4.3货到工地款：货到工地支付70%，在设备国债补贴公示发给3天内付清，若国债未能发给，则在货到工地之日起15个月内由代理垫付清。”
3、**设备到货前x天支付**的条款属于“提货款” 
  案例: "设备到货前15天，提交经建设单位认可的付运文件后，建设单位支付给供应商该批次电梯供应金额的20%"； 该条款应该是 提货款：20% `而不是` 预付款：20%
4、移交款......支付审定总价... 属于 结算完成 节点
  案例: "4.3移交款：通过审价后在收到发票后买方（委托方）向本合同约定的卖方账户支付审定总价...."；该条款应该是 结算完成 节点。
5、案例：「最终验收付款(20%)：验收单或验收报告出具并且发包人收到质量保证金保函后十个工作日内，依据发票支付剩余货款。」 → 优先归 当地政府部门验收后（验收触发条件优先于"余款"措辞）
6、有预付款保函及定金的情况如：“甲方收到乙方提供的预付款保函后30天内，向乙方支付合同总价10%的订金”应属于“销售定金”而不是“预付款”
7、**工程整体竣工**：凡涉及“专项维修基金”、“政府补贴”、“国债补贴”、“审计审价完成”等关键词，通常归为此类（除非明确受限于“货到工地”）。
8、**货到工地**：-   关键词：“交货验收”、“开箱验收”。
9、**提货款**：凡涉及“发货前支付”、“设备到货前X天支付”，均归为此类，**不是**预付款。
10、**结算完成**：涉及“移交款…支付审定总价.... ”、“办理结算手续后”。
11、**电梯移交用户后**：关键词：“移交物业”、“移交钥匙”。
"""


# ===== 安装付款条款标准节点列表 =====
_INSTALL_STANDARD_NODES = "1.定金 | 2.进场前（首付）（关键词：\"开工前\",\"设备进场前\",\"书面进场通知\"） | 3.进场后（关键词：\"合同设备进场后\",\"设备进场\"） | 4.移交前 | 5.报验前 | 6.公司验收后（关键词：\"厂家验收\"，\"乙方验收\"，\"甲方验收\"） | 7.当地政府部门验收后（关键词：\"政府验收\", \"特种设备验收\", \"技监部门验收\", \"监督检验合格\"） | 8.电梯移交用户后（关键词：\"移交用户\", \"交付使用\"） | 9.工程整体竣工 | 10.特殊付款-移交前 | 11.特殊付款—移交后 | 12.质保金（关键词：\"质量保证金\", \"尾款\"）"

_INSTALL_OUTPUT_EXAMPLES = """**单节点条款示例**（仅包含一个付款动作）：
```json
[
  {{"payment_type": "进场前（首付）", "ratio": "30%", "amount": null}}
]
```

**多节点条款示例**（包含多个独立付款动作）：
```json
[
  {{"payment_type": "定金", "ratio": "10%", "amount": null}},
  {{"payment_type": "公司验收后", "ratio": "60%", "amount": null}},
  {{"payment_type": "质保金", "ratio": "5%", "amount": null}}
]
```"""
_INSTALL_JUDGEMENT ="""
1、书面进场通知 → 进场前（首付）
2、厂家/甲方/乙方验收 → 公司验收后  
"""

# ===== 最终拼装：设备 / 安装 各自独立的提示词 =====
EQUIPMENT_PAYMENT_RATIO_PROMPT = _PAYMENT_RATIO_PROMPT_COMMON.format(
    standard_node_list=_EQUIPMENT_STANDARD_NODES,
    output_examples=_EQUIPMENT_OUTPUT_EXAMPLES,
    judgement=_EQUIPMENT_JUDGEMENT,
)

INSTALL_PAYMENT_RATIO_PROMPT = _PAYMENT_RATIO_PROMPT_COMMON.format(
    standard_node_list=_INSTALL_STANDARD_NODES,
    output_examples=_INSTALL_OUTPUT_EXAMPLES,
    judgement=_INSTALL_JUDGEMENT,
)

# 向后兼容：保留 PAYMENT_RATIO_PROMPT 别名，默认指向设备版本
PAYMENT_RATIO_PROMPT = EQUIPMENT_PAYMENT_RATIO_PROMPT



PAYMENT_SUMMARY_RATIO_PROMPT = """# 设备付款条款批量复核与标准化

## 1. 角色与目标
你是精通合同、法律与财务的分析专家。对输入的批量**设备侧**付款条款进行：过滤 → 计算 → 金额 → 合并去重 → 类型标准化 → 校验，全链路处理后输出结构化 JSON。

## 2. 核心铁律（违反即任务失败）
1. **原文不可改**：`payment_clause` 逐字来自输入，禁止改写/截断/编造。
2. **ID 对应**：输出 `id` 必须来自输入，不得新造或遗漏。
3. **数量控制**：输出节点数 ≤ 输入节点数，只能"过滤 / 合并"，不得拆分。
4. **禁止外部引用**：`chunk_text_map` 与 `rag_examples` 仅供理解，禁止出现在输出字段。
5. **过程可追溯**：数组**最后**一个对象必须为 `{{"thinking_output": "..."}}`。

## 3. 处理流水线

### Step 1 | 无效节点过滤（命中任一即丢弃）
1. **0 元一票否决**：文本含 `0元 / 零元 / RMB0元 / 零元整` 等明示金额为 0。
   - 例："5%的合同定金,即人民币(大写)：零元整元整(RMB0元" → 丢弃。
2. **保函类**：各类保函（预付款/履约/质保/保修）的提交、替换、返还。
   - 例："甲方有权要求乙方重新开具合同价3%的保修保函" → 丢弃。
3. **仅总价**：只描述总价，无具体支付节点。
   - 例："产品含税总价：¥443,240元。" → 丢弃。
4. **空泛付清**：仅含"付清合同约定的应付款项 / 付清余款"，无条件或比例。
   - 例："委托方付清合同约定的应付款项" → 丢弃。
5. **概述引导**：仅描述付款安排方式，无具体金额/比例/单次动作。
   - 例："分期分批供货、付款、结算" → 丢弃。
   - 例："产品如分批次验收，委托方按每批次比例分批支付施工费余额" → 丢弃（仅描述分摊方式，无具体比例/金额）。
6. **付款凭证/通知类程序条款**：主要内容为付款后的凭证传真/寄送、注明合同编号、以银行凭证为准等手续性描述，即使含"支付"字样也丢弃。
   - 例："委托方应在付款后，将付款凭证传真或寄送至受托方的合同经办人，并注明付款单位名称、合同编号" → 丢弃。
7. **残缺片段**：语义不完整，无法独立成节点。
   - 例："求的,甲方有权不支付款项。" → 丢弃。
8. **拒付类**：含"不支付 / 有权拒付 / 暂停支付"等否定表述 → 丢弃。

### Step 2 | 比例计算（累计还原）
- "支付**至** X%" 或 "结清**至** X%"：`本次 final_ratio = X% − 前序已保留节点累计比例`（结果 ≥ 0）。
- 其他情况：直接取原文比例。

### Step 3 | 金额核算
- 有 `reported_amount` → 直接使用其值。
- 无 `reported_amount` → `final_amount = null`。
- 金额为 0 的节点一律过滤（与 Step 1.1 一致）。

### Step 4 | 合并与去重
1. **质保金强制合并**：所有 `payment_type = 质保金` 的节点合并为一个——`final_ratio` 累加、`final_amount` 累加、`payment_clause` 用 `\\n` 拼接、保留首个 `id`。
2. **多合同号去重**：若多节点 `payment_type + final_ratio` 相同（仅合同号不同），仅保留一个代表性节点。
   - 例：合同号 `25N3S08-PTP` 和 `25N3S08-PTL` 都是"提货款 95%" → 合并为单个"提货款 95.0%"。

### Step 5 | 支付类型标准化
**标准类型（共 13 类，按优先级从左到右）**：
1.销售定金 | 2.预付款 | 3.提货款 | 4.货到工地 | 5.安装后 | 6.公司验收后 | 7.当地政府部门验收后 | 8.结算完成 | 9.特殊（移交前） | 10.电梯移交用户后 | 11.特殊（移交后） | 12.工程整体竣工 | 13.质保金

**特殊判定（优先级高于字面关键词匹配）**：
- **销售定金**：含"预付款保函" + "定金/订金"的组合（例："甲方收到乙方预付款保函后30天内支付合同总价10%的订金"）→ 销售定金，非预付款。
- **提货款**：含"发货前支付 / 设备到货前 X 天支付"（例："设备到货前15天支付该批次电梯供应金额的20%"）→ 提货款，非预付款。
- **货到工地**：关键词"交货验收 / 开箱验收"。**例外**：最终限制条件为"货到工地"时，即使涉及国债补贴仍归此类（例："货到工地支付70%,若国债未发给则由代理垫付"）。
- **工程整体竣工**：涉及"专项维修基金 / 政府补贴 / 国债补贴 / 审计审价完成"（除非触发"货到工地"例外）。
  - 例："政府验收完毕取得国债补贴及维修基金审批完毕后支付100%安装款" → 工程整体竣工。
- **当地政府部门验收后**：关键词"政府验收 / 特种设备验收 / 技监部门验收 / 安装验收合格"；"进度款"若上下文为安装验收阶段亦归此类（例："进度款：再向乙方支付进度款,即总价10%的合同款"）。
- **结算完成**：涉及"移交款…支付审定总价.... / 办理结算手续后"（例："通过审价后…支付审定总价扣除已支付款项后的余款"）。
- **电梯移交用户后**：关键词"移交物业 / 移交钥匙"。

### Step 6 | 最终校验
- `final_ratio` 之和应为 100%；微小误差在**最后一个**节点微调。
- 过滤 `final_ratio ∈ {{0, null}}` 的节点。
- 过滤 `final_amount ∈ {{0, "0", null 之外的 0 值}}` 的节点。

## 4. 输入数据
- `{batch_payment_items}`：待处理节点列表。
- `{chunk_text_map}`：条款上下文，**仅用于理解判断，禁止引用内容**。
- `{rag_examples}`：参考示例，**仅用于理解任务/格式，禁止引用其内容**。

## 5. 输出格式（严格遵守）
整个输出是**一个** JSON 代码块 ` ```json ... ``` `，代码块内为 JSON 数组；数组元素依次为：**N 个付款节点对象 + 1 个 thinking_output 对象**。

**付款节点对象严格只含 5 个字段**（多一个、少一个均视为违规）：
```json
[
  {{
    "id": "输入的原始id",
    "payment_clause": "原文逐字复制",
    "payment_type": "13 种标准类型之一",
    "final_ratio": "如 30.0%；无则 null",
    "final_amount": "金额字符串；无则 null"
  }},
  {{
    "thinking_output": "纯文本思考过程：Step1过滤→Step2计算→Step3金额→Step4合并→Step5分类→Step6校验"
  }}
]
```

## 6. JSON 语法硬约束（违反将导致解析失败）
1. **禁止悬挂逗号**：任何 `}}` 或 `]` 前一个非空白字符不得是 `,`。
   - 错误：`{{ "a": 1, }}` 、`[ {{...}}, ]`
2. `thinking_output` **仅**以单个对象形式出现在**数组最后位置**；禁止拆多份、禁止散落 `Step X:` 裸文本进数组。
3. 数组中除 `thinking_output` 对象外，**禁止**出现任何"非条款对象"（说明/元信息对象）。
4. 字符串内换行必须转义为 `\\n`，禁止裸换行。
5. **输出前自检**：以上 4 条逐条确认 → 再输出。
"""



INSTALL_PAYMENT_SUMMARY_RATIO_PROMPT = """# 安装付款条款批量复核与标准化

## 1. 角色与目标
你是资深工程合同（尤其**安装工程**侧）与财务分析专家。对输入的批量安装付款条款进行：过滤 → 合并 → 计算 → 金额 → 类型标准化 → 校验，全链路处理后输出结构化 JSON。

## 2. 核心铁律（违反即任务失败）
1. **0 元一票否决（最高优先级）**：任一条款含 `0元 / 零元 / RMB0`（含"定金 0 元"等），**Step 1 直接丢弃**，不得参与后续合并、计算或输出。
2. **原文不可改**：`payment_clause` 逐字来自输入（合并节点为原文直接拼接），禁止改写/补充。
3. **禁止外部引用**：`chunk_text_map`、`rag_examples` 仅供理解，禁止出现在输出字段。
4. **过程可追溯**：数组**最后**一个对象必须为 `{{"thinking_output": "..."}}`。

## 3. 处理流水线

### Step 1 | 无效节点过滤（命中任一即丢弃）
1. **0 元一票否决**：含 `0元 / 零元 / RMB0`，无论是否含比例，直接丢弃。
2. **保函类**：各类保函（预付款/履约/质保）的提交/替换/返还，非现金交易。
3. **空泛付清**：仅"付清应付款项 / 费用包含……"，无金额或比例。
4. **纯定义**：仅描述总价、责任、赶工费，无支付动作。
5. **被动/假设**：如"如因……则应……"之类的条件陈述，无实际支付。
6. **补贴/资金来源说明**：仅描述政府补贴、维修基金、垫付方式，且**无明确金额或比例**。
7. **概述引导**：仅描述付款方式（如"分期分批供货、付款、结算"、"根据工程建设进度付款"、"按每批次比例分批支付施工费余额"），无具体金额/比例/单次动作。
   - 例："产品如分批次验收，委托方按每批次比例分批支付施工费余额" → 丢弃（仅描述分批支付的分摊方式，无具体比例/金额）。
8. **付款凭证/通知类程序条款**：主要内容为付款后的凭证处理、通知、传真寄送、银行凭证确认等手续（即使含"支付"字样），必须丢弃。
   - 例："委托方应在付款后，将付款凭证传真或寄送至受托方的合同经办人，并注明付款单位名称、合同编号" → 丢弃。
   - 例："双方约定本合同施工费委托方支付至受托方分管分公司的账号，委托方已支付款项以银行付款凭证为准" → 丢弃。
9. **残缺片段**：语义不完整，无法独立成节点（如"求的,甲方有权不支付款项。"）。
10. **拒付类**：含"不支付 / 有权拒付 / 暂停支付"等否定表述。

**有效节点判定（必须同时满足）**：
- 具备明确付款条件或时间；
- 至少含其一（且排除 0 元）：明确金额 / 明确百分比 / 可计算术语（余额、尾款、余款）。

### Step 2 | 逻辑合并（仅在通过过滤的条款之间）
- 同一付款事件（条件一致、上下文连续）合并为一个节点；
- 保留首个 `id`，`payment_clause` 为原文直接拼接。

### Step 3 | 比例计算与校验
- 按输入顺序处理；
- "支付**至** X%" → `本次 final_ratio = X% − 前序累计`；
- 累计比例 ≤ 100%，误差在最后一个节点微调；所有计算过程记入 `thinking_output`。

### Step 4 | 金额提取（仅对有效节点）
- 优先级：原文直接金额（非 0）→ `init_amount` → `total_amount × final_ratio`（若两值均有效）→ `null`。

### Step 5 | 支付类型标准化
**安装合同标准类型（共 12 类，按优先级从左到右）**：
1.定金 | 2.进场前（首付） | 3.进场后 | 4.移交前 | 5.报验前 | 6.公司验收后 | 7.当地政府部门验收后 | 8.电梯移交用户后 | 9.工程整体竣工 | 10.特殊付款-移交前 | 11.特殊付款-移交后 | 12.质保金

**字面关键词映射**：
- **进场前（首付）**：`开工前 / 设备进场前 / 书面进场通知`
- **进场后**：`合同设备进场后 / 设备进场`
- **公司验收后**：`厂家验收 / 甲方验收 / 乙方验收`
- **当地政府部门验收后**：`政府验收 / 特种设备验收 / 技监部门验收 / 监督检验合格`
- **电梯移交用户后**：`移交用户 / 交付使用 / 移交物业 / 移交钥匙`
- **质保金**：`质量保证金 / 尾款`

**特殊判定（优先级高于字面匹配）**：
1. **工程整体竣工**：涉及"专项维修基金 / 政府补贴 / 国债补贴 / 审计审价完成"时，**仅当条款直接约定向乙方支付的金额或比例**才归此类；否则按 Step 1.6（补贴说明）过滤。
2. 厂家/甲方/乙方验收 → **公司验收后**（而非当地政府部门验收后）。
3. 书面进场通知 → **进场前（首付）**。

### Step 6 | 合并去重与最终校验
- **质保金强制合并**：多个 `payment_type = 质保金` 节点合并为一个，`final_ratio` 累加（例 2×1.5% → 3%）、`final_amount` 累加、`payment_clause` 用 `\\n` 拼接、保留首个 `id`。
- **同类去重**：除质保金外若仍出现多节点同 `payment_type`，说明 Step 5 分类错误，按语义重判并去重。
- `final_ratio` 之和应为 100%；微小误差在最后一个节点微调。
- 过滤 `final_ratio ∈ {{0, null}}` 与 `final_amount = 0` 的节点。

## 4. 输入数据
- `{batch_payment_items}`：待处理条款列表。
- `{chunk_text_map}`：条款上下文，**仅用于理解判断，禁止引用内容**。
- `{rag_examples}`：参考示例，**仅用于理解任务/格式，禁止引用其内容**。

## 5. 输出格式（严格遵守）
整个输出是**一个** JSON 代码块 ` ```json ... ``` `，代码块内为 JSON 数组；数组元素依次为：**N 个付款节点对象 + 1 个 thinking_output 对象**。

**付款节点对象严格只含 5 个字段**（多一个、少一个均视为违规）：
```json
[
  {{
    "id": "输入的原始id",
    "payment_clause": "原文逐字复制（合并节点为原文拼接）",
    "payment_type": "12 种安装标准类型之一",
    "final_ratio": "如 30.0%；无则 null",
    "final_amount": "具体金额字符串，不得是百分比或小数比例；无则 null"
  }},
  {{
    "thinking_output": "纯文本思考过程：Step1过滤→Step2合并→Step3计算→Step4金额→Step5分类→Step6校验"
  }}
]
```

## 6. JSON 语法硬约束（违反将导致解析失败）
1. **禁止悬挂逗号**：任何 `}}` 或 `]` 前一个非空白字符不得是 `,`。
   - 错误：`{{ "a": 1, }}` 、`[ {{...}}, ]`
2. `thinking_output` **仅**以单个对象形式出现在**数组最后位置**；禁止拆多份、禁止散落 `Step X:` 裸文本进数组。
3. 数组中除 `thinking_output` 对象外，**禁止**出现任何"非条款对象"（说明/元信息对象）。
4. 字符串内换行必须转义为 `\\n`，禁止裸换行。
5. **输出前自检**：以上 4 条逐条确认 → 再输出。
"""

WARRANTY_SUMMARY="""# 角色
你是一个严格遵守规则的合同条款分析专家。你的唯一任务是对输入的质保期条款进行分析，并按照指定格式最终输出最正确的质保期信息。

# 核心业务规则
1.  **对象锁定**: 只提取**整机/主设备**的质保信息。忽略“易损件”、“配件”、“电池”等附属部件。
2.  **优先提取逻辑**:
    *   当条款包含双重期限（如“A事件起X个月，但最迟不超过B事件起Y个月”）时：
    *   `warranty` = **X** (优先提取生效后的时长)。
    *   `closed_end_conditions` = **B事件起Y个月** (闭环/截止描述)。
3.  **单位强制转换**:
    *   所有时长必须统一转换为 **“个月”**。
    *   例如：“1年” -> “12个月”，“2年” -> “24个月”， “5年” -> “60个月”。

# 绝对输出规则
- **绝对规则1: Markdown代码块**: 你的**整个输出**必须严格地包裹在一个JSON代码块中，像这样: ` ```json ... ``` `
- **绝对规则2: 必须是数组**: 代码块内部的根结构**必须是**一个JSON数组。它**必须**以 `[` 开头，并以 `]` 结尾。
- **绝对规则3: 数组内容**: 这个数组**必须**包含：
    1.  一个或多个代表**质保信息**的JSON对象（通常为1个）。
    2.  **数组的最后一个元素**，**必须是**一个包含`"thinking_output"`键的JSON对象。

**最终输出结构必须严格如下所示:**
```json
[
  {{
    "warranty": "24个月",  // 必须以'个月'结尾
    "effective_conditions": "质保期生效/起算条件描述",
    "closed_end_conditions": "闭环/截止条件描述(若无则为空字符串)"
  }},
  {{
    "thinking_output": "纯文本格式的思考过程，解释为何提取该时长以及如何进行的单位换算"
  }}
]
```

# 参考示例
## 示例条款1
Input: "9.3由卖方或卖方委托方负责安装的产品，在买方和使用管理方遵守安全技术规范合理正常的保管和使用下，产品质量保修期（质保期）为合同设备经当地特种设备管理部门监督检验合格之日起的36个月，但最迟不超过本合同第五条确认出货日期起的42个月。"
Output:
```json
[
  {{
    "warranty": "36个月",
    "effective_conditions": "合同设备经当地特种设备管理部门监督检验合格之日",
    "closed_end_conditions": "最迟不超过本合同第五条确认出货日期起的42个月"
  }},
  {{
    "thinking_output": "1. 识别双重期限：'检验合格起36个月'(优先) vs '出货起42个月'(闭环)。2. 提取warranty：取优先项'36个月'。3. 字段映射：'检验合格...'填入生效条件，'最迟不超过...'填入闭环条件。"
  }}
]
```
## 示例条款2
Input: "- 5)保修期期限：设备安装竣工验收合格后免费维修保养2年，产品质量保修2年 - 6)免费质保期期限起计方式：从最终验收合格并由质监站和技术监督局签发《电梯准用证》和《电梯安全准用证》交付买方使用之日起计算"
Output:
```json
[
  {{
    "warranty": "24个月",
    "effective_conditions": "从最终验收合格并由质监站和技术监督局签发《电梯准用证》和《电梯安全准用证》交付买方使用之日起计算",
    "closed_end_conditions": ""
  }},
  {{
    "thinking_output": "1. 识别时长：原文'2年'。2. 单位换算：2 * 12 = '24个月'。3. 识别条件：结合条款5和条款6，起算点为条款6描述的完整内容。4. 闭环判断：无'最迟不超过'字样，闭环条件留空。"
  }}
]
```

# Input Data
- `{warranty_clauses}`：质保期条款。
"""

RESULT_VERIFICATION_PROMPT="""# 付款条款去重

## 任务
对输入条款按 payment_type 去重。**相同 clause_category + payment_type 的多个条款，只保留最合适的一个，丢弃其余。**

## 去重选择规则（按优先级）
1. 优先保留包含具体支付动作（"支付""付款"）且有金额/比例的条款
2. 优先保留条款文本更长、信息更完整的
3. 过滤0元条款（含"0元""零元"或金额为0）

## 输入
- `validation_equipment`: {validation_equipment}
- `validation_install`: {validation_install}

## 输出要求
只输出JSON数组，无其他文字。每个 payment_type 只输出一个ID。
```json
[{{"select_clause_id":"保留的ID"}},{{"thinking_output":"去重理由"}}]
```

## 示例
输入：id="0_3" payment_type="验收后" clause="验收后三天内支付18465元" 和 id="0_4" payment_type="验收后" clause="分批次验收分批支付余额"
输出：[{{"select_clause_id":"0_3"}},{{"thinking_output":"0_3含具体金额和时间节点，信息更完整，丢弃0_4"}}]

错误示例：同时输出0_3和0_4 → 错误！相同payment_type只能保留一个。

输出："""

PAYMENT_CLAUSE_VALIDATION_PROMPT = """# 付款条款有效性验证

## 任务
判断"待验证条款"本身是否为有效付款条款。
有效付款条款必须在文本中**至少存在一处**同时包含：①正向支付动作（"支付""付款"等）+ ②具体金额或比例（X万元、X%等数值）。
条款可能较长，包含多个付款节点（如分期付款：首期款10%、出货款60%、验收款30%），只要其中**任意一个节点**满足①+②即为有效。
全文均不含①+②则 is_valid=false。

## 直接排除（命中任一 → is_valid=false）
- 含"违约金/罚款/赔偿/赔偿损失/违约责任"
- 含"逾期"且含"利息/利率/滞纳金"
- 含"质量问题/质量缺陷"
- 含"有权不支付/拒付/暂停支付"
- 含"保函/担保函"但无"支付X%/X元"的实际付款
- 仅描述价格/报价/总价/单价，无支付动作（如"设备总价为XX万元"或"安装费XX万元"）
- 仅描述支付方式/工具，无金额比例（如"采用商业承兑汇票""以银行转账方式支付"）
- 仅描述付款前置条件/依据/验收要求，无支付动作和金额（如"以XX证明为付款的必要依据""验收合格后方可付款"）。**注意**：如果条件描述中同时包含具体比例/金额和付款动作（如"验收合格后一次性100%全额付款"），则为有效付款条款，不应排除
- 仅描述供货期/交货期/工期等时间安排，无支付动作
- 概述性/引导性条款，无具体金额（如"合同价款按以下方式分期支付"、"按每批次比例分批支付施工费余额"——仅描述分摊方式而无具体比例/金额）
- 付款凭证/通知类程序条款：主要内容为付款后的凭证传真/寄送、注明合同编号、以银行凭证为准等手续性描述，即使含"支付"字样也排除（如"委托方应在付款后将付款凭证传真或寄送至受托方的合同经办人"）
- 指引条款（如"详见第X条""在专用条款第X条中规定"）
- 单据要求（如"正本X份，副本X份"）
- 附件/目录/表格标题

## 示例
✅ "支付供方设备总价的20%作为定金" → true（支付+20%+合同款）
✅ "向供方支付该批次设备总价的40%的提货款" → true（支付+40%+合同款）
✅ "支付给供方该批设备总价的20%" → true（支付+20%+合同款）
✅ "合同生效且甲方货物下单后10个工作日内，甲方支付该批次下单货物合同额的10%作为该批次货物首期款；分批产品交货前15个工作日内，甲方支付该批次设备总额的60%的出货款；...甲方向乙方支付该项目设备总额的30%的验收款" → true（多节点分期付款，含多个支付+比例）
✅ "政府验收合格后一次性100%全额付款" → true（付款+100%，条件与付款动作一体化描述）
✅ "备注：...本项目系政府验收合格后一次性100%全额付款，乙方取得政府部门监督检验合格报告后向甲方请款" → true（虽以备注开头，但含具体付款比例100%和付款动作）
❌ "以政府部门出具的验收合格证明为付款的必要依据" → false（付款前置条件，无支付动作和金额）
❌ "合同签订、预付款已付之日起计算供货期" → false（供货期描述，无支付动作）
❌ "合同价款按以下方式分期支付" → false（概述引导，无具体金额）
❌ "支付合同总价1%的违约金" → false（违约金）
❌ "发包人将竣工验收款支付给承包人" → false（无具体金额或比例）
❌ "设备总价为人民币500万元" → false（仅价格，无支付动作）
❌ "卖方应提供合同价10%的履约保函" → false（保函，非实际支付）
❌ "采用商业承兑汇票支付" → false（仅支付方式，无金额比例）
❌ "产品如分批次验收，委托方按每批次比例分批支付施工费余额。为确保受托方及时确认委托方支付的施工费款项，按时安排进场、施工、验收和交付，委托方应在付款后，将付款凭证传真或寄送至受托方的合同经办人，并注明付款单位名称、合同编号" → false（仅描述分批支付的分摊方式+付款凭证处理程序，无具体比例/金额）
❌ "委托方应在付款后，将付款凭证传真或寄送至受托方的合同经办人，并注明付款单位名称、合同编号" → false（付款凭证处理程序）

## 输出
只输出JSON对象，无其他文字。
```json
{{"id": "输入的id", "is_valid": true/false, "reason": "简短理由"}}
```

## 待验证条款
`{validation_clauses}`

输出："""


# ===== 混签付款条款归属判定（仅对 clause_class 含 混签付款条款 的条款触发） =====
PAYMENT_CLAUSE_CATEGORY_PROMPT = """# 混签付款条款归属判定

## 任务
对下方"待判定条款"判断其归属，三选一：`equipment_payment` / `installation_payment` / `both`。

条款正文通常**没有章节标题**（如"### 4.1设备"/"### 4.2安装"），判定必须**同时结合 clause 正文与 clause_context 上下文**进行关键词与语义综合分析。

## 判别规则（按优先级从上到下）

### 规则 0：合计总价主体 → `both`（最高优先级，优先于规则 1/2）
**判定核心**：付款比例/金额直接挂钩的【主体】是合计总价词，且无设备/安装限定词修饰主体，则一律 `both`。
- 合计总价词：`合同总价 / 合同总金额 / 产品总价 / 审定总价 / 货款总额 / 项目总价 / 项目款项 / 工程总价 / 设备及安装总价 / 总金额 / 尾款总金额`
- **关键区分**：条款里出现"设备/安装"字样时，必须判断它是【主体限定】还是【时间/条件修饰】：
  - 主体限定（属规则 1/2）：`设备总价的 X%`、`安装款 X%`
  - 时间/条件修饰（仍属规则 0）：`设备验收后支付审定总价 X%`、`设备进场前支付合同总价 X%`——主体仍是"审定总价/合同总价"
- 即使条款含"预付款/质保金/竣工款"等节点名，只要主体是合计总价词，仍判 `both`，不被节点名误导。

### 规则 1：设备关键词命中 → `equipment_payment`
- 金额/比例描述：`设备总价的X%`、`合同设备价格`、`合同设备款`、`设备款的X%`
- 付款节点名：`预付款`、`提货款`、`货到工地款`、`销售定金`、`出货款`
- 物流动作：`发货前`、`到货`、`交货前`、`设备运抵`

### 规则 2：安装关键词命中 → `installation_payment`
- 金额/比例描述：`安装总价的X%`、`施工总价的X%`、`工程总价的X%`、`施工费`
- 付款节点名：`安装款`、`进场款`、`进场后`、`开工款`
- 施工动作：`脚手架`、`施工`、`调试`、

### 规则 3：同时存在两类独立节点 → `both`
- 条款内同时出现独立的"设备款 X%" 和 "安装款 Y%" 两类付款动作
- 跨类别"尾款/质保金"且上下文同时覆盖设备和安装两条线

### 规则 4：统一单节点 或 判不清 → `both`（默认兜底，信息无损）
- 设备/安装关键词均未命中，或上下文信号矛盾
- **只要归属分不清，一律兜底为 `both`**（双轨展开，避免漏节点）

## 示例
- "买方向卖方支付产品总价的30.00%的合同款" → `both`（规则 0：主体"产品总价"，"预付款"是节点名不是主体限定）
- "自电梯验收合格12个月后支付审定总价的5.00%质保金" → `both`（规则 0：主体"审定总价"，"电梯验收"是时间条件不是主体限定）
- "安装完毕通过政府验收之后30日之内支付尾款总金额的20%即54694元" → `both`（规则 0：主体"尾款总金额的20%"，"安装完毕"是时间条件不是主体限定）
- "向乙方支付设备总价50%的预付款" → `equipment_payment`（规则 1：主体含"设备总价"限定）
- "支付相当于合同总价50%的安装款" → `installation_payment`（规则 2：节点名"安装款"明确限定）
- "设备进场后支付施工总价的30%" → `installation_payment`（规则 2：主体含"施工总价"限定）
- "合同生效后支付设备款10%；设备进场后支付安装款5%" → `both`（规则 3：双类独立节点）
- "预付款：甲方及丙方定于合同签订后15个工作日内将合同总价30%预付款"` → `both`（合同总价）

## 输出
只输出 JSON 对象，无其他文字，`category` 必须是三个枚举之一，**禁止 null / 空字符串**。
```json
{{"id": "0", "category": "equipment_payment", "reason": "简短理由（不超过30字）"}}
```

## 待判定条款
`{category_clause}`

输出："""

# ===== 单组去重（每次只处理一个 (clause_category, payment_type) 组，单任务单输出） =====
RESULT_VERIFICATION_SINGLE_GROUP_PROMPT = """# 付款条款单组去重

## 任务
下方"候选条款"是**同一 clause_category + 同一 payment_type**的 N 个重复条款，只保留**最合适的一个**，输出其 id。

## 选择规则（按优先级）
1. 同时包含支付动作（"支付""付款""付清""汇入"）和具体金额或比例（X%/X万元/X元）的优先
2. 条款文本更长、信息更完整的优先
3. 含"0元""零元"或金额为 0 的不要选
4. 若难以判断，选第一条（列表中的首个 id）

## 候选条款
`{group_clauses}`

## 输出
只输出 JSON 对象，无其他文字。`select_clause_id` **必须**是候选条款中存在的 id，禁止新造 id。
```json
{{"select_clause_id": "候选中的ID", "reason": "简短理由（不超过30字）"}}
```

输出："""


# ===== 导出所有提示词 =====
__all__ = [
    "PAYMENT_RATIO_PROMPT",
    "EQUIPMENT_PAYMENT_RATIO_PROMPT",
    "INSTALL_PAYMENT_RATIO_PROMPT",
    "PAYMENT_SUMMARY_RATIO_PROMPT",
    "INSTALL_PAYMENT_SUMMARY_RATIO_PROMPT",
    "WARRANTY_SUMMARY",
    "RESULT_VERIFICATION_PROMPT",
    "RESULT_VERIFICATION_SINGLE_GROUP_PROMPT",
    "PAYMENT_CLAUSE_VALIDATION_PROMPT",
    "PAYMENT_CLAUSE_CATEGORY_PROMPT",
]
